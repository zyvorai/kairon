# Roadmap

## v0.3 — secure migration control plane

All shipped -- predates the `[x]` convention adopted later in this file, not an open milestone.

- [x] mTLS node-to-node prepare/commit/abort protocol
- [x] atomic destination session journal and idempotency
- [x] no user-supplied migration transport URI
- [x] local Unix-socket migration adapter contract
- [x] rollback before commit and `NeedsRecovery` after ambiguous commit
- [x] adopt-only target cutover
- [x] cold migration, CSI snapshots, DRA/VFIO guard retained

## Network Fabric — FluxVM eBPF edge

Kairon declares VM-edge networking; FluxVM owns TAP/TC/eBPF; Fabric proxies dataplane UX.
See [`docs/network-fabric.md`](docs/network-fabric.md).

- [x] **N1** Machine network create parity (`forwards`, `macvtapMode`, `staticNetwork`, `podUID`) + dataplane status projection -- `internal/model/network.go`'s `NetworkSpec`, mapped by `internal/agent/network.go` into FluxVM's own `POST /v1/vms` payload and `status.network.*` projection.
- [x] **N2** `MachineNetworkPolicy` + `NetworkSecurityGroup` → FluxVM policy/groups/CNP -- `charts/kairon/crds/machinenetworkpolicies.yaml`/`networksecuritygroups.yaml`, upserted per-node by `internal/agent/network.go` against `POST /v1/vms/{id}/network/policy`/`POST /v1/network/groups`.
- [x] **N3** Live-migration network quiesce / export / restore / resume -- `internal/fluxvm/network.go`'s `NetworkMigrationQuiesce`/`Export`/`Restore`/`Resume` against FluxVM's `…/network/migration/{quiesce,export,restore,resume}`, driven by `internal/migration`'s source/destination adapter around the existing prepare/commit protocol.
- [x] **N4** Service Fabric VIP membership + `dataplaneRequired` fail-closed readiness -- `internal/agent/network.go`'s `reconcileServiceFabric` registers the resolved guest IP as a named-service backend once known; `spec.network.dataplaneRequired` fails Machine readiness closed rather than silently reporting healthy when the dataplane attach itself is unhealthy.
- [x] **N5** FluxVM `dataplaneMode` (`legacy|ebpf|cilium`) on `Machine.spec.network`, mapped into the create payload and fail-closed when `dataplaneRequired` and attach is unhealthy.
- [x] **N6** Opt-in Cilium cluster-network attach via `CiliumExternalWorkload` (`network.ciliumAttach.enabled`, `spec.network.ciliumAttach`) — raw REST, no Cilium Go SDK. Multus stays non-primary.
- [x] **N7** Opt-in `MachineNetworkPolicy.spec.cilium.sync` → namespaced `CiliumNetworkPolicy`, plus `kaironctl network status`.

## v0.4 — FluxVM live-migration backend

- [x] implement the Kairon migration adapter in FluxVM or a companion host service (`cmd/kairon-migration-adapter-fluxvm`)
- [x] QEMU incoming destination lifecycle and QMP transfer implementation
- [x] authenticated/encrypted data-plane transport (`migration.dataplaneTls`, `status.dataPlaneEncrypted`). Since v0.5, `migration.dataplaneTlsSecretName` (opt-in) gives every node a real per-node data-plane cert instead of the shared control-plane cert it fell back to before -- `scripts/gen-migration-mtls-certs.sh` generates both the certs and a ready-to-apply Secret, each `kairon-node` pod's init container picks out only its own node's files (matched via the Downward API), and fails closed rather than falling back to a shared/wrong identity if its own entry is missing. See `docs/guides/machine-migration-tls.md`.
- [x] cancellation and operator recovery commands (`kaironctl recover`, `docs/runbook-migration-failures.md`)
- [x] shared-storage and block-migration preflight (first cut, opt-in `kairon.zyvor.dev/storage-domain`/`network-domain` node labels — see `docs/guides/machine-fencing.md`)
- [x] migration network selection and bandwidth policy

## Operational tooling (shipped ahead of schedule)

Not originally scoped for a specific version, but small enough to land alongside the v0.4 work above:

- [x] Prometheus metrics + example alert rules (`internal/metrics`, `charts/kairon/alerts.yaml`)
- [x] `kairon-ui` web dashboard (`cmd/kairon-ui`, `internal/uiapi`, `web/`)
- [x] `kairon-ui` real per-operator username/password login (bcrypt accounts, signed sessions, audit attribution, Helm-generated default admin, rate-limited/lockout on repeated failed attempts) — closes what was the dashboard's longest-standing known auth gap; see SECURITY.md
- [x] `spec.cloudInit` and `spec.network.forwards` ergonomics (`kaironctl create --hostname/--ssh-key/--forward/...`, matching dashboard fields) — see `docs/guides/machine-network.md`
- [x] Graphical VNC console (`console.enabled`): `kairon-ui` → `kairon-node` → the VM's local QEMU VNC socket, rendered in-browser via noVNC — FluxVM exposes no remote VNC endpoint of its own, so this is a real relay Kairon built, not a wrapper. Tickets are bound to the requesting username with a full open/close audit trail, the button hides itself when ineligible, the kairon-ui↔kairon-node hop optionally runs over one-way TLS (`console.tls.enabled`), and `scripts/deploy-remote.sh --with-console` covers the bare-metal install path — see SECURITY.md's "VNC console" section
- [x] `internal/integration`: a CI-runnable controller+agent pipeline test -- since extended with scale/failure-injection/upgrade suites, see the "upgrade/scale/failure-injection test suites" entry below
- [x] multi-host migration test and `NeedsRecovery` drill runbooks (`docs/runbook-multi-host-migration-test.md`, `docs/runbook-recovery-drill.md`) — documented and scripted; still not yet run against real hardware in this repo's own CI, which remains a real, named gap, not a false completion claim

## v0.5+

- [x] PVC -> FluxVM disk lifecycle (first cut): `spec.volumes[0]` boots from a Bound PVC's `hostPath`/`local` PersistentVolume — see `docs/guides/machine-storage.md`. Still open: multiple volumes per Machine.
- [x] CSI node-plugin integration for network-block volumes (first cut, one backend): `csiNode.enabled` deploys Kairon's own iSCSI-only CSI node plugin (`internal/csinode`). The second deliberate exception to Go-stdlib-only (`google.golang.org/grpc`, `github.com/container-storage-interface/spec`) -- the CSI protocol itself has no stdlib-only wire format. `kairon-node` dials it directly as its own CSI client rather than through kubelet's Pod volume machinery, since a Machine has no backing Pod. No Ceph/EBS/other backends, no CHAP on Kairon's own consumption path (`kairon-node`'s own iSCSI login, distinct from the CSI Controller's own dynamic-provisioning path below). Runs privileged, on a non-distroless base image. See `docs/guides/machine-storage-csi.md`.
- [x] CSI Controller for dynamic iSCSI provisioning, with CHAP and volume expansion (first cut): `csiController.enabled` adds a real `ControllerServer` (`internal/csinode/controller.go`) driving a Linux LIO iSCSI target directly -- `CreateVolume`/`DeleteVolume` provision/tear down a real portal/IQN/LUN per `PersistentVolumeClaim`, indistinguishable from a statically hand-written PV once `NodeServer`'s own `NodeStageVolume` consumes it. A provisioner Secret's username/password (`storageClass.parameters.csi.storage.k8s.io/provisioner-secret-*`) configures real LIO CHAP auth instead of demo mode; `ControllerExpandVolume` grows an existing LUN in place. Real limits: one backing file per volume on whichever single node runs `kairon-csi-controller` (no topology-aware placement across multiple storage nodes); `CreateSnapshot`/`DeleteSnapshot` support is a separate entry below. See `docs/guides/machine-storage-csi.md`.
- [x] CSI `CreateSnapshot`/`DeleteSnapshot` for dynamically-provisioned volumes (first cut): `ControllerServer` now implements both (`internal/csinode/controller.go`), closing what was previously this driver's own explicitly-documented gap. Since a dynamically-provisioned volume is just a flat file under `csiController.volumeDir`, a snapshot is a real file clone (`cp --reflink=auto` -- an instant, metadata-only copy-on-write clone on a filesystem that supports it, e.g. btrfs/XFS-with-reflink, a real byte-for-byte copy otherwise, coreutils' own automatic fallback) into a `.snapshots/` subdirectory, and restoring a new volume from one (`CreateVolume`'s `volume_content_source`) copies it back out before the usual LIO backstore/target/LUN setup runs. Requires the cluster's own `external-snapshotter` stack (the `snapshot.storage.k8s.io` CRDs plus its cluster-wide `snapshot-controller` -- neither installed by this chart, same as every other CSI driver) and the new, separately-gated `csiController.snapshotter.enabled` (off by default, since the sidecar fails to start at all without those CRDs already present). No cross-volume dedup, no incremental/differential snapshots -- each is an independent full clone. See `docs/guides/machine-storage-csi.md`.
- [x] `MachineSnapshotRestore` (first cut): restores one `MachineSnapshot` volume into a new PVC via the standard CSI `dataSource` flow — "restore" and "clone-from-snapshot" are the same operation in Kubernetes' storage model, so this covers both. Deliberately doesn't also create a Machine (point the existing PVC-backed-disk feature at the result instead) — see `docs/guides/machine-snapshot-restore.md`. Needs a real CSI snapshotter behind your StorageClass (confirmed on a real cluster: Rancher's `local-path-provisioner` doesn't have one -- "snapshotting non-CSI volumes is not supported").
- [x] CPU/memory hotplug (first cut): growing `spec.resources` on a Running Machine calls FluxVM's real QMP hotplug (`device_add`/`object-add`, shipped upstream in FluxVM's own `feat/qemu-cpu-memory-hotplug`, PR #62) — see `docs/guides/machine-hotplug.md`. Grow-only (no CPU/DIMM unplug), bounded by headroom reserved automatically at creation (not yet Kairon-configurable), lost across any Machine re-realization (stop/start, cold migration).
- [x] guest-agent (qemu-guest-agent) integration: `spec.guestAgent.enabled` resolves `status.guestIP` via FluxVM's real `guest-network-get-interfaces` (shipped upstream, [zyvorai/fluxvm#63](https://github.com/zyvorai/fluxvm/pull/63)) for network modes with no DHCP lease file (`user`/SLIRP in particular) — see `docs/guides/machine-guest-agent.md`. Graceful-shutdown wiring turned out to be unnecessary: FluxVM's `stop`/`delete` already does ACPI `system_powerdown` with a wait-then-force fallback regardless of guest agent presence. Multi-NIC/IPv6 (`status.guestIPs`) and periodic re-resolution (every 5 minutes once first resolved, instead of once-ever) closed the two gaps this used to list as still open.
- [x] Machine affinity/anti-affinity: `spec.placement.affinity`/`antiAffinity` (required/hard) gate scheduling against other Machines' current placement. Since v0.5, `internal/scheduler` also does real weighted scoring on top of the existing least-loaded/hash-tiebreak baseline: `preferredAffinity`/`preferredAntiAffinity` (soft, weighted), `topologySpreadConstraints` (soft, minimizes matching-Machine count per topology domain; `maxSkew` is also hard-enforced per constraint via `whenUnsatisfiable: DoNotSchedule`, mirroring the Kubernetes field), and a best-effort DRA topology-awareness hint (reads back an already-allocated `ResourceClaim`'s device pool via `ResourceSlice.spec.nodeName`, no role in allocation itself). A Machine using none of the new fields schedules identically to before this scoring pass existed. See `docs/guides/machine-placement.md`.
- [x] `MachineDisruptionBudget`: `evacuate` throttles itself against `minAvailable`/`maxUnavailable` instead of migrating an entire node at once — see `docs/guides/machine-disruption-budgets.md`. Since v0.5, an opt-in validating admission webhook (`webhook.enabled`) also rejects a `MachineMigration` created directly through the API if it would violate a budget — closing the "only gates `kaironctl evacuate`" gap this line used to describe. `kairon-controller` now also reconciles real `status` (`expectedMachines`/`currentHealthy`/`desiredHealthy`/`disruptionsAllowed`) every tick, mirroring a real PodDisruptionBudget's status and closing what used to be documented as "no live status, no controller for this object" — purely observational, `evacuate`/the webhook still each recompute their own allowance fresh rather than trusting it. `evacuate --wait` now keeps retrying (matching `kubectl drain`'s own retry model) until the node is actually empty or `--timeout` elapses, instead of a single pass that left budget-blocked Machines behind for good -- the closest thing to an automatic node-drain Kairon has, deliberately still operator-invoked rather than triggered by `node.spec.unschedulable` on its own.
- [x] `MachineQuota` (namespace-scoped): `maxMachines`/`maxTotalCpu`/`maxTotalMemory`, enforced in `kairon-controller`'s scheduling loop — see `docs/guides/machine-quotas.md`. Since v0.5, the same opt-in webhook above also rejects a `Machine` create that would immediately exceed a quota (matching the reconcile loop's own CREATE-only scope), instead of only leaving it `Pending` — and now also rejects an update that hotplug-resizes an already-scheduled Machine's `spec.resources` past quota, closing what was a real, documented gap (`kairon-node`'s hotplug reconciliation has no cluster-wide quota visibility of its own, so this is the one quota check with no reconcile-loop equivalent to backstop it). `migration.maxConcurrentPerNode`/`maxConcurrentCluster` (a per-node/cluster concurrency cap on in-flight migrations specifically) remains a separate, narrower control this doesn't replace.
- [x] `kairon-ui` in-place password management: self-service change + admin reset (`POST /api/v1/auth/password`, `POST /api/v1/users/{username}/password`), replacing "regenerate a bcrypt hash and redeploy" for named `ui.auth.users` accounts.
- [x] `scripts/deploy-remote.sh --console-tls`: automates (or accepts bring-your-own) TLS material for the kairon-ui↔kairon-node console relay hop, previously a manual step.
- [x] Helm chart hardening: `PodDisruptionBudget` (on by default) and opt-in `NetworkPolicy` for `kairon-controller`/`kairon-ui`.
- [x] Supply chain: Dockerfile base images pinned to a digest, Trivy image scanning in CI, and a tag-triggered `release.yml` that publishes scanned, digest-pinned images to `ghcr.io/zyvorai/kairon-*` — this repo's CI is now the source of those tags.
- [x] SR-IOV/GPU migration capability checks (preflight only, first cut): `internal/controller/preflight.go`'s `deviceClaimsPreflight` refuses live migration of a `spec.deviceClaims` Machine outright -- VFIO passthrough is boot-time-only, and QEMU's live-migration RAM transfer fundamentally cannot carry a passthrough PCI device's in-flight state across hosts without a hot-unplug/hot-plug bracket FluxVM doesn't expose today (it has `HotplugCPU`/`HotplugMemory` but no VFIO equivalent) -- turning a previously silent gap (the device was just left behind) into an explicit, honest rejection instead. Cold migration is allowed when the target node asserts an equivalent device via the new opt-in `kairon.zyvor.dev/vfio-devices` label (the same "operator-asserted, can only confirm a mismatch" honesty `storage-domain`/`network-domain` labels already have) -- cold migration has no state-transfer problem, since the guest is stopped and a fresh runtime created at the target exactly like initial creation. Actually carrying a device through *live* migration remains blocked on FluxVM gaining real VFIO hot-unplug/hot-plug support -- a genuine upstream dependency, not an unpicked Kairon TODO.
- [x] Guest quiesce hooks for snapshots (first cut): a Machine with `spec.guestAgent.enabled` now gets a real `guest-fsfreeze-freeze`/`guest-fsfreeze-thaw` around `MachineSnapshot`'s VolumeSnapshot creation, for an application-consistent snapshot instead of a merely crash-consistent one -- the same mechanism libvirt/virt-manager already use. This needed new support on both sides: FluxVM itself had no `fsfreeze` endpoint at all (its QGA surface was narrow and purpose-built -- network-interfaces, exec, firewall -- though it already had the low-level `qga::raw` plumbing this reuses), so `POST /v1/vms/{id}/qga/fsfreeze`/`fsthaw` were added there first. On Kairon's side, `kairon-controller` (which reconciles `MachineSnapshot`) has no network path to a Machine's FluxVM instance at all -- only `kairon-node` does -- so freeze/thaw is coordinated via a request/response annotation pair on the Machine itself (`kairon.zyvor.dev/quiesce-request`/`quiesce-status`, `internal/controller/snapshot.go` + `internal/agent/quiesce.go`), the same "durable request in the API, not memory" pattern `kairon.zyvor.dev/adopt-only` already established for controller↔node coordination during live migration. A freeze that never confirms within 30s falls back to an unquiesced, crash-consistent snapshot rather than blocking forever; a thaw that fails is retried indefinitely, never silently abandoned, matching `NeedsRecovery`'s own posture. QEMU backend only. See `docs/guides/machine-snapshot-quiesce.md`.
- Cluster API infrastructure provider ("Kubernetes clusters made of Kairon Machines") -- scoped, not built: this is a separate project (its own controller-manager/repository/release, following `cluster-api-provider-<name>` convention), not a Kairon feature PR. Real blocker, not just size: CAPI's whole object graph is built on `metadata.ownerReferences`, confirmed absent everywhere in this codebase (`model.ObjectMeta` has no such field; every parent-child relationship today is a plain name/label reference the reconcile loop resolves itself, e.g. `MachineSet`'s own template-hash label). Two real fits worth keeping if this is ever built: CAPI bootstrap-data Secrets could plausibly forward straight into `spec.cloudInit` with little new plumbing, and `MachineSet`'s own "create/delete to match a template" reconciliation shape is conceptually close to what CAPI expects from an infra provider. See `docs/design-cluster-api-provider.md`.
- [x] TLS certificate hot-reload (`internal/tlsreload`): migration mTLS, the VNC console relay, and the admission webhook all now pick up a renewed leaf certificate/key (e.g. from cert-manager) within 30s, no restart. Only the leaf cert/key -- a CA bundle still loads once at startup. Automated issuance (ACME/cert-manager integration Kairon itself drives) and SPIFFE-style workload identity remain open.
- [x] node fencing (first cut): `kairon-controller` sets a `NodeUnreachable` condition on any Machine whose node stops being `Ready`, every reconcile tick -- detection only, never automatic rescheduling (risks running the same VM twice). `kaironctl fence --reason ...` is the explicit operator-attested action that clears a Machine for rescheduling once a human has confirmed out-of-band the node is truly gone -- see `docs/guides/machine-fencing.md`.
- confidential VM policy (SEV-SNP/TDX) -- verified, still blocked upstream: FluxVM has since (see below) implemented real UEFI Secure Boot/vTPM, but SEV-SNP/TDX are a separate, still entirely-unimplemented gap (confirmed against FluxVM's own source, not assumed -- zero references to SEV/SNP/TDX/confidential-computing anywhere in its crates, a genuine "no firmware/attestation support at all" gap distinct from the OVMF/swtpm work that closed `secureBoot`/`.tpm`). Deliberately not adding a new `spec.security` field for this yet: inventing API surface specifically so it can always be rejected would be speculative, not a real capability with a real caller behind it. Revisit once FluxVM has real SEV-SNP/TDX support to expose.
- [x] OIDC/SSO for `kairon-ui` (first cut): `ui.oidc.enabled`, Authorization Code + PKCE against an external identity provider, alongside `ui.auth.users`. The one deliberate exception to Kairon's Go-stdlib-only design (`golang.org/x/oauth2`, `github.com/coreos/go-oidc/v3`) -- real JWT/JWK verification isn't something to hand-roll. No group-to-admin claim mapping, no SP-initiated single-logout, no refresh-token renewal. See `docs/guides/kairon-ui-oidc.md`.
- [x] multi-replica `kairon-ui` (first cut): `ui.replicaCount > 1` propagates session revocation, login lockout, console tickets, and password changes across replicas via a shared, Kubernetes-native `ConfigMap` (deliberately not Redis) -- eventually-consistent (~15s), not instant; login-lockout's failure count is per-replica, not one cluster-wide atomic counter; concurrent password changes to two different accounts on two different replicas can still race. See `docs/guides/kairon-ui-ha.md`.
- [x] `kairon-controller` HA (first cut): `controller.replicaCount > 1` closes what was previously this project's clearest single point of failure -- a `coordination.k8s.io/v1` Lease (`internal/leaderelection`, hand-rolled on top of `internal/kube` rather than pulling in `client-go`'s leader-election package) ensures only the elected leader's reconcile loop runs at a time; the admission webhook and health/metrics endpoints are unaffected, serving from every replica regardless. On by default in the Helm chart even at `replicaCount: 1`; off by default at the binary/flag level for backward compatibility with existing bare-metal deployments. Takeover after a lost leader costs up to ~15s, not instant. See `docs/guides/kairon-controller-ha.md`.
- [x] upgrade/scale/failure-injection test suites (first cut): `internal/integration` (already home to the controller+agent migration pipeline test) gains three more real, in-process suites, each driving actual reconcile loops rather than mocking around them. **Scale** (`scale_test.go`): a `MachineSet` scaled 0→12→5 through `Controller.Reconcile`'s real, full multi-concern loop (not `reconcileMachineSet` called directly, which `internal/controller/machineset_test.go` already covers in isolation) -- proving created replicas actually flow through scheduler assignment to real nodes, evenly. **Failure injection**: a fake Kubernetes API server that fails a MachineSet's first create call once (`scale_test.go`) proves the fault surfaces in `status.message` and the next tick makes up the shortfall with no duplicates; a fake FluxVM that goes fully unreachable (`failure_injection_test.go`, covering both of `Agent.current`'s ID- and name-lookup paths together, since either alone is already masked by the other's fallback) proves a Machine surfaces `Error`/`Ready=False` during the outage and recovers to `Running` on its own the next tick, no operator action needed. **Upgrade** (`internal/model/compat_test.go`): a Machine JSON blob in an older, pre-this-session shape (missing every field added since) still decodes cleanly, and one in a hypothetical *newer* shape (extra fields this binary doesn't know about yet) decodes and round-trips without leaking or losing anything -- the real backward/forward-compatibility guarantee `docs/guides/crd-versioning.md` describes but had never been asserted in code. Building the FluxVM-outage test caught a real, separate latent bug along the way, fixed in the same commit: `MachineStatus.Message` and six sibling status `Message` fields were tagged `omitempty`, which silently dropped the field from every JSON merge-patch once a reconcile tried to clear it back to `""` -- RFC 7386 treats an *absent* key as "leave unchanged," not "clear," so a recovered Machine/Migration/Snapshot/etc. would have kept showing its last error message forever. Now covered by its own direct regression test (`TestStatusMessageFieldsAlwaysSerialize`), not just caught incidentally. Real limits: no upgrade test yet exercises an actual live Kubernetes API server's own CRD schema validation (`kubectl apply` of an old/new object against the real `openAPIV3Schema`) or a real Helm-chart `helm upgrade`; scale is proven into the low tens of Machines, not load-tested at production scale; failure injection covers FluxVM-unreachable and one apiserver-create-failure shape, not every possible fault (e.g. a slow/timing-out apiserver, a partial write).
- [x] Backup/restore for Kairon's Kubernetes-level state (first cut): `scripts/backup-crds.sh`/`restore-crds.sh` back up and restore the eight `kairon.zyvor.dev` CRD kinds, plus opt-in chart-managed Secrets -- closing what was previously a real, undocumented gap (no backup/DR story existed at all). Restoring a Machine whose FluxVM runtime is still alive re-adopts it by name (`RuntimeName()`, not `status.runtimeID`); if the runtime is also gone, a fresh VM is created from `spec.image`/`spec.resources` instead -- whether that's a real recovery depends on whether the boot disk itself survived independently. Does not cover VM disk content (the CSI driver's/storage backend's job) or FluxVM's own per-host state. See `docs/runbook-backup-restore.md`.
- [x] Observability beyond migrations (first cut): Prometheus metrics used to be `kairon-controller`-only and migration-lifecycle-only -- no signal at all if the controller was unhealthy or wedged, no metrics from `kairon-node`/`kairon-ui` at all. Now every component exposes `kairon_apiserver_request_duration_seconds` (every `internal/kube.Client` call, by method/outcome); `kairon-controller`/`kairon-node` also expose `kairon_reconcile_duration_seconds`/`kairon_reconcile_errors_total`; `kairon-controller` also exposes `kairon_webhook_decisions_total` when `webhook.enabled`; `kairon-ui` exposes its own `kairon_ui_request_duration_seconds`. Three new alerts (`charts/kairon/alerts.yaml`'s `kairon-health` group) on reconcile error rate, apiserver error rate, and webhook deny-rate spikes -- the `PrometheusRule` this optionally renders is renamed `kairon-alerts` (from `kairon-migrations`) to match, see `docs/guides/observability.md`.
- [x] `kairon-ui` per-address rate limiting (first cut): every route now bounds request volume per remote address (`-rate-limit-rps`/`-rate-limit-burst`, on by default), via a new hand-rolled `internal/ratelimit` token bucket -- stdlib-only, so this didn't need a third exception to Go-stdlib-only after OIDC and CSI. Distinct from, and in addition to, the existing per-username login lockout: this bounds volume from one client address against *any* route, not just repeated failed passwords against one username. Deliberately doesn't cover the admission webhook (would risk rejecting a legitimate bulk `kubectl apply` under `failurePolicy: Fail`) or inter-component RPCs (already mTLS/bearer-token-restricted to trusted peers, not open to arbitrary traffic). Keyed by `r.RemoteAddr` alone -- collapses to one bucket for every client behind a load balancer with no `X-Forwarded-For` trust configured, a real known limit. See SECURITY.md.
- [x] Per-Machine VNC console authorization (first cut): the opt-in `kairon.zyvor.dev/console-allowed-users` annotation (comma-separated operator usernames) closes what was previously an all-or-nothing model -- any operator who could see a Machine could open its console. Unset (the default) is unchanged; set, `handleConsoleTicket` denies ticket issuance to anyone not listed, with an admin account (`ui.auth.users[].admin`) always able to bypass it for break-glass access, and a caller with no real per-operator identity (the legacy shared `ui.token`, or unauthenticated dev mode) denied outright rather than silently allowed. Bundled with a fix to bind each console ticket to the one Machine it was issued for -- without it, a ticket for a Machine the caller can access could be replayed against one they can't, within the ticket's 30s TTL. FluxVM's own VNC socket remains unauthenticated as-is; this closes the authorization gap at the kairon-ui layer, not the underlying trust chain. See SECURITY.md's "VNC console" section.
- [x] CRD conversion webhook scaffold (first cut): new `internal/conversion` hand-rolls the `apiextensions.k8s.io/v1` `ConversionReview` wire format, the same kind of deliberate exception `internal/admission` already makes for `AdmissionReview` -- no client-go dependency. `kairon-controller`'s existing webhook server exposes a tested `POST /convert/machinequotas` on the same TLS listener/certificate/`Service` as the admission webhook, backed by a real worked example (`ConvertMachineQuota`: renames `MachineQuota.spec.maxTotalCpu`/`maxTotalMemory` and the matching `status` fields to `maxCpu`/`maxMemory` between a hypothetical `v1beta1` and today's `v1alpha1`), proven via unit tests and a real end-to-end HTTP `ConversionReview` test. No CRD actually registers a second version yet -- this is infrastructure ready to wire in, not a live version bump; it also names, rather than solves, one real remaining gap: `charts/kairon/crds/*.yaml` lives in Helm's never-templated `crds/` directory, so a live `caBundle` for a real `spec.conversion` needs a deliberate follow-up choice. See `docs/guides/crd-versioning.md`.
- [x] Real Kubernetes RBAC for console access (first cut): `ui.auth.rbacConsoleCheck` (opt-in) layers a real `authorization.k8s.io/v1` `SubjectAccessReview` (verb `get` on the `machines/console` subresource) on top of the existing `kairon.zyvor.dev/console-allowed-users` annotation allowlist -- closing a real gap versus KubeVirt's aggregated-API-server subresource RBAC, without any CRD changes (`kubectl create clusterrole --resource=machines/console --verb=get` already works today; RBAC resource strings with a subresource suffix are just strings the authorizer compares, the same `nodes/proxy` precedent). Only meaningful for an identity `kube-apiserver`'s own RBAC recognizes (an OIDC claim mapped into its own OIDC config); a local `ui.auth.users[]` account is denied outright when this is on, since it has no real Kubernetes `User` to check. See `docs/guides/kairon-ui-console-rbac.md`.
- [x] Automatic cordon-triggered evacuation (first cut): `controller.cordonEvacuation.enabled` (opt-in, off by default) makes `kairon-controller` itself create a `MachineMigration` for every Machine on a Node whose `spec.unschedulable` transitions to true, mirroring KubeVirt's `LiveMigrateIfPossible` eviction strategy -- closing what was previously a real, documented gap ("no background/automatic drain triggered by `node.spec.unschedulable`"). Reuses the exact `MachineDisruptionBudget` throttling `kaironctl evacuate`/the admission webhook already share (`internal/controller/cordon.go`); a budget-blocked or already-attempted Machine is retried at most once per `controller.cordonEvacuation.minRetryInterval` (default `2m`), tracked via a `kairon.zyvor.dev/cordon-evacuate-attempted-at` annotation. `controller.cordonEvacuation.strategy` is `cold`/`auto` only -- deliberately never `live`, since automatic live migration triggered with no operator watching is a bigger step than automatic cold. See `docs/guides/machine-disruption-budgets.md`.
- [x] `MachineSet` (first cut): a Deployment/ReplicaSet-shaped abstraction -- `kairon-controller` creates/deletes plain `Machine` objects (`internal/controller/machineset.go`) to match `spec.replicas`, each built from `spec.template` (exactly a `Machine`'s own `spec`, validated the same way, not re-declared field-by-field in the CRD schema -- see `charts/kairon/crds/machinesets.yaml`'s use of `x-kubernetes-preserve-unknown-fields`). No `ownerReference`/garbage-collection (this project has none anywhere) -- owned Machines are found via a `kairon.zyvor.dev/machineset` label, the same name/label-reference pattern `MachineMigrationSpec.MachineName` already established for a single parent-child link. `RollingUpdate` (default, `maxUnavailable`-bounded, reusing `MachineDisruptionBudget`'s own `ParseIntOrPercent`) and `Recreate` rollout strategies both replace outdated replicas (tracked via a `kairon.zyvor.dev/machineset-template-hash` label) rather than editing one in place. Every owned Machine stays a completely ordinary Machine -- `kaironctl evacuate`, `MachineDisruptionBudget` selectors, everything else keeps working unchanged. Originally paced by count, not health (a rollout didn't wait for a freshly created replacement to become `Running` before continuing) -- since closed, see the readiness-gating entry below. Still no `maxSurge`, autoscaling, or pause/rollback primitive. See `docs/guides/machine-sets.md`.
- [x] `MachineInstanceType` (first cut): a reusable, named CPU/memory shape (`spec.resources`) a Machine references via `spec.instanceTypeName` instead of inlining `spec.resources` itself -- Kairon's equivalent of an EC2 instance type or KubeVirt's `VirtualMachineInstancetype`, deliberately kept to just the resource shape (no OS-preference bundling). `kairon-controller` resolves it into the Machine's own `spec.resources` exactly once (`internal/controller/instancetype.go`), the same "creation-time-only" convention `spec.image`/`spec.network.forwards` already use -- an explicit `spec.resources` always wins, and nothing downstream (hotplug, quota, the admission webhook, `kairon-node`) needs to know an instance type was ever involved once resolved. Relaxes `Machine.spec.resources`'s previously-required `cpu`/`memory` fields (now optional at the CRD-schema level, same relaxation `spec.image.path` already got for `spec.image.source`) since either `resources` or `instanceTypeName` may now supply them. Namespace-scoped only, no admission-time validation of the reference (an unresolvable name just stays unresolved rather than being rejected up front), no `kairon-ui` picker yet. See `docs/guides/machine-instance-types.md`.
- [x] NUMA topology, CPU set, and hugepages (first cut, qemu-only): `spec.resources.numaNode`/`.cpuSet`/`.hugepages` are direct passthroughs to FluxVM's own already-existing `CreateVmRequest` fields of the same name -- genuinely new plumbing on Kairon's side, not blocked upstream the way VFIO-through-live-migration was. `internal/fluxvm.Client.CreateWithVFIO` refuses the Machine outright with a clear error when any of the three are set and the resolved backend isn't `qemu`, rather than letting FluxVM silently ignore them the way it does for Cloud Hypervisor/Firecracker. **Not** exclusive host-core pinning -- `cpuSet` is a guest-visible vNUMA topology hint only; real host `cgroup cpuset.cpus` pinning exists in FluxVM (its own resize API) but allocating specific, non-overlapping host CPU numbers across competing Machines on one node is a real capacity-allocation problem `internal/scheduler`'s still-pure-count-based bin-packing doesn't solve, and remains a separate, bigger, not-yet-designed feature -- see `docs/guides/machine-cpu-numa.md`.
- [x] Windows guest support (first cut, honest scope): legacy-BIOS Windows Server/Windows 10 already works today with a pre-built image carrying VirtIO drivers and [cloudbase-init](https://cloudbase-init.readthedocs.io/) -- `spec.cloudInit` needs zero changes, since cloudbase-init consumes the exact same NoCloud datasource FluxVM's own cloud-init support already generates. `spec.security.secureBoot`/`.tpm` (previously dead fields -- accepted by the CRD, wired to nothing, doing nothing) are now refused outright at `CreateWithVFIO` with a clear error instead of a silent no-op: confirmed against FluxVM's own source, no backend Kairon talks to (QEMU, Cloud Hypervisor, Firecracker) implements UEFI/OVMF firmware or a vTPM device today, so Windows 11 (which hard-requires both) isn't supported -- a genuine FluxVM-side capability gap, tracked here the same way VFIO-through-live-migration names its own FluxVM dependency rather than reading as an unpicked Kairon TODO. See `docs/guides/machine-windows-guests.md`.
- [x] `MigrationPolicy` (first cut): scopes migration bandwidth defaulting and concurrency to Machines matching `spec.selector`, independent of the global `migration.maxConcurrentPerNode`/`maxConcurrentCluster` Helm values -- Kairon's namespace-scoped equivalent of KubeVirt's own `MigrationPolicy` CRD. Enforced by the existing migration reconcile loop's own admission check (`internal/controller/migrationpolicy.go`, mirroring `MachineDisruptionBudget`'s `LoadBudgetStates`/`AdmitDisruption` shape exactly) -- no admission webhook needed. `spec.maxConcurrent` blocks an over-cap migration into `Blocked`, retried automatically; `spec.bandwidthMbps` only fills in a `MachineMigration`'s own `spec.bandwidthMbps` when it's unset (an explicit per-migration value always wins), first-matching-policy-wins when more than one applies, deliberately not merged. See `docs/guides/migration-policies.md`.
- [x] Velero compatibility (verified, no plugin needed): a generic Velero backup/restore of Kairon's CRDs works with no Kairon-specific plugin -- confirmed by code review (not yet drilled live in this repo's own CI): no finalizer blocks a restore (Velero restore only ever `Create`s, never `Delete`s), a restored Machine re-adopts its FluxVM runtime automatically via the same reconcile-loop logic `runbook-backup-restore.md` already documents, and every Kairon `Patch*` call is a scoped JSON merge patch that never wipes Velero's own tracking labels. Velero's standard CSI-snapshot mechanism still cannot back up disk content for the far more common `hostPath`/`local` PV case (never a real CSI volume in the first place) -- but Kairon's own iSCSI CSI driver *does* now implement `CreateSnapshot`/`DeleteSnapshot` for dynamically-provisioned volumes (see below), the exact limit this bullet used to name having since been closed for that one path. See `docs/runbook-velero-backup.md`.
- [x] `kubectl-kairon` plugin (first cut): `kubectl kairon ...` now works identically to `kaironctl ...`, via a real second binary (`cmd/kubectl-kairon`) sharing kaironctl's exact command dispatch -- `kaironctl`'s own `run()` was factored out into an importable `internal/kaironctl` package (`Run(args, version)`) so both binaries call the same code, rather than one shelling out to the other. This works because kubectl already invokes a plugin binary with the subcommand as its own `os.Args[1:]` (`kubectl kairon evacuate worker-1` runs `kubectl-kairon evacuate worker-1`), exactly the argument shape `kaironctl`'s dispatch already expected -- no argument translation needed. Pure ergonomics, no new backend surface; not shipped as a container image, matching `kaironctl` itself.
- [x] VM export (documented, deliberately not new Kairon machinery): getting a Machine's disk content out of the cluster entirely (cross-cluster clone, off-site backup) is standard Kubernetes tooling -- a short-lived Pod mounting the same `PersistentVolumeClaim` read-only, `kubectl cp`/pushed to object storage -- not a new Kairon-specific export server/ticket mechanism. Kairon has no Pod/Job/Service creation anywhere (`kairon-node` never runs a Machine inside a Pod at all), and building bespoke export infrastructure would only duplicate what a one-off Pod already does, at the cost of real new API surface (Pod scheduling, Service exposure, its own RBAC) this project doesn't otherwise need. See `docs/runbook-vm-export.md`.
- [x] Image import (first cut): `spec.image.source.httpURL` (opt-in via `node.imageCacheDir`, off by default) has `kairon-node` itself download a remote qcow2/raw image URL into a node-local, digest-keyed cache instead of requiring an operator to have already placed a file at `spec.image.path` -- closing Kairon's most direct gap against KubeVirt's CDI importer. `spec.image.digest` (already existing) becomes the mandatory cache key, so every Machine naming the same digest shares one download. No format conversion (FluxVM already consumes qcow2/raw directly, so bytes are trusted via digest, not inspected), no OCI/registry references, no cache eviction -- see `docs/guides/machine-image-import.md`.
- [x] Guest exec (first cut): a `Running` Machine with `spec.guestAgent.enabled` gets an **Exec** button in `kairon-ui`'s dashboard -- run a one-shot command (or PowerShell one-liner for Windows guests) inside the guest with no SSH key, console, or network path into the guest required. This wraps FluxVM's real `POST /v1/vms/{id}/qga/exec` (qemu-guest-agent guest-exec, with FluxVM doing its own guest-exec-status polling internally -- Kairon only ever sees one synchronous request/response) via the exact same `kairon-ui` &harr; `kairon-node` relay infrastructure the VNC console already established (`internal/consoleproxy`), just a plain JSON POST instead of a WebSocket byte relay. No new spec field, no new guest-image dependency -- reuses the same channel `spec.guestAgent` already provides for guest-IP resolution and `MachineSnapshot` quiesce. Gated more strictly than the console though: requires an admin account outright (no per-Machine allowlist opt-out yet), and there's no command-line audit trail by design (arguments can carry secrets) -- see SECURITY.md's "Guest exec" section and `docs/guides/machine-guest-exec.md`. One-shot only, no interactive shell -- see the text console entry below for that.
- [x] Interactive text console (first cut): `spec.guestAgent.console` opts a Machine into FluxVM's own vsock-based interactive shell (`GET /v1/vms/{id}/console`, Kairon's `virtctl console` equivalent), rendered in-browser via xterm.js -- `kairon-ui` &harr; `kairon-node` &harr; FluxVM, reusing the exact same ticket-issuance, per-Machine authorization, and `KAIRON_NODE_CONSOLE_TOKEN`/`console.tls` trust chain the VNC console already established, just a `kind=text` ticket dialing a different `internal/consoleproxy` route (kairon-node as a *client* dialing FluxVM's own WebSocket console endpoint, rather than a Unix socket it dials directly the way VNC's QEMU display works). Independent of `spec.guestAgent.enabled` (standard `qemu-guest-agent`) -- this is a completely different, FluxVM-**proprietary** channel requiring FluxVM's own `fluxvm-guest-agent` binary and systemd service baked into the guest image, a genuinely new operational dependency this project has never asked of users before. Works on every backend (unlike VNC, QEMU-only), since FluxVM's vsock agent isn't tied to a display device; the in-guest token itself is entirely FluxVM's own to generate and inject, never touched by Kairon. See SECURITY.md's "Text console" section and `docs/guides/machine-text-console.md`. No resize-after-connect, no session persistence/reconnect, and the xterm.js bundle is lazy-loaded in the dashboard so it never costs anything for a deployment that doesn't use this feature.
- [x] Live host resource limits (first cut): `spec.resources.limits` (`cpuQuotaPercent`/`memoryMaxBytes`/`ioWeight`/`pidsMax`) is a real, kernel-enforced cgroup v2 cap on a running Machine's own VMM process -- FluxVM's `POST /v1/vms/{id}/resources`, backend-agnostic (unlike `hugepages`/`numaNode`/`cpuSet`, this isn't QEMU-only, since it operates on the cgroup the VMM process runs in, not a backend-specific API). Distinct from `spec.resources.cpu`/`.memory` (what the *guest* sees) and from CPU/memory hotplug (which grows what the guest sees, one-way only): this is a host-side ceiling, reconciled continuously and freely raisable-or-lowerable at any time, closing a real gap versus KubeVirt (whose VMs get Kubernetes' own Pod resource `limits` enforcement for free; a Kairon Machine, running outside a Pod entirely, previously had no host-side enforcement at all beyond the scheduler's bin-packing count). Deliberately excludes FluxVM's own `cpuset_cpus` cgroup control -- the same real capacity-allocation problem already named in `spec.resources.cpuSet`'s own doc comment (allocating specific, non-overlapping host cores across competing Machines needs a scheduler capacity model this project doesn't have). See `docs/guides/machine-resource-limits.md`.
- [x] Configurable hotplug headroom (first cut): `spec.resources.maxCpu`/`.maxMemory` request more CPU/DIMM hotplug headroom than FluxVM's own default (roughly double `cpu`, and `memory + 2Gi` or double, whichever is larger) -- a real, previously self-documented gap (`docs/guides/machine-hotplug.md` said outright "not yet configurable from Kairon"; FluxVM's own `CreateVmRequest.max_vcpus`/`.max_memory_mib` already existed and were simply never plumbed through). Creation-time-only, like `cpu`/`memory`'s own boot-time sizing -- the headroom is baked into the VM's boot-time `-smp`/`-m` arguments (`maxcpus=`/`maxmem=`) and can't be grown on an already-running Machine at all, so this doesn't lift `CPU/memory hotplug`'s own "bounded by headroom reserved at creation" limit, it just lets that fixed ceiling be set higher upfront. FluxVM silently clamps `maxCpu` up to at least `cpu` if set too low (never an error); a `maxMemory` below `memory` is FluxVM/QEMU's own inconsistent-argument error to raise, not something Kairon validates first. See `docs/guides/machine-hotplug.md`.
- [x] `spec.cloudInit.writeFiles` (first cut): drops a file into the guest before first boot via cloud-init's own `write_files` module -- e.g. a systemd unit or an app config, no custom image build needed. Another previously self-documented gap: FluxVM's own `CloudInitSpec.write_files` already existed (`internal/fluxvm.CloudInitSpec`'s own doc comment said outright "not yet surfaced here") and was simply never plumbed through. Same creation-time-only posture as the rest of `spec.cloudInit`. See `docs/guides/machine-network.md`'s "Cloud-init guest customization" section. No `kaironctl create` flag yet -- YAML/kubectl only, matching how most multi-field `cloudInit` entries already work.
- [x] Guest file access (first cut): a `Running` Machine with `spec.guestAgent.console` gets a **Files** button in `kairon-ui`'s dashboard -- read or write a file inside the guest at runtime, no SSH key or shared volume needed. Wraps FluxVM's own bespoke vsock guest agent (`POST /v1/vms/{id}/agent/put-file`/`agent/get-file` -- the same channel and guest-image dependency, `fluxvm-guest-agent`, the interactive text console needs; a completely different channel from guest exec's `qemu-guest-agent`), relayed through the same `internal/consoleproxy` infrastructure as a plain JSON request/response, the same shape guest exec already has. Admin-only, same posture as guest exec (stricter than the text console's any-operator default) -- arbitrary guest file read/write is at least as sensitive as arbitrary command execution. No path allowlist/denylist, and reads are capped at kairon-ui's own ~3MB effective ceiling (base64 inflates a 4MiB HTTP-response limit) -- a larger file fails to decode with a clear error rather than returning truncated content. See SECURITY.md's "Guest file access" section and `docs/guides/machine-guest-agent-files.md`.
- [x] Pause/resume (first cut): `spec.powerState: Paused` suspends a running Machine's guest CPUs via FluxVM's real QMP `stop`/`cont` -- backend-agnostic (unlike CPU/memory hotplug's QEMU-only restriction), since pause/resume is implemented across QEMU, Cloud Hypervisor, and Firecracker alike. RAM and device state stay fully resident; resuming (`spec.powerState: Running`) continues exactly where the guest left off, no re-attach or boot. Kairon's `virtctl pause`/`unpause` equivalent -- `kaironctl pause`/`resume` (and `kubectl kairon` identically), or the dashboard's **Pause**/**Resume** buttons, all sharing the exact same generic `spec.powerState` merge-patch `start`/`stop` already use. Deliberately doesn't create a runtime that doesn't exist yet -- pausing only applies to a Machine already realized against FluxVM at least once, matching KubeVirt's own "pause an existing VMI" semantics rather than "create one paused from the start". A Paused Machine still counts against the scheduler's per-node capacity (`countAssigned`), unlike Stopped, since the VMM process and its resident RAM are still there. See `docs/guides/machine-pause-resume.md`.
- [x] Network policy read-back, guest fsfreeze-status, and guest firewall toggle (first cut): three more previously-unwrapped FluxVM endpoints closed as part of a full route-by-route audit against `internal/fluxvm`. `GET /v1/vms/{id}/network/policy` (`fluxvm.GetVMNetworkPolicy`) is now read after every `MachineNetworkPolicy` apply and compared against what was sent -- `status.effectiveSynced` is a real confirmation now, not just "the POST call returned success" (see `docs/guides/network-policy.md`). `GET .../qga/fsfreeze-status` and `POST .../qga/firewall/{open,close}` are exposed as admin-only, `spec.guestAgent.enabled`-gated REST endpoints alongside guest exec (same `internal/consoleproxy` relay shape) -- API-only for now, no dedicated dashboard button yet, matching how `spec.cloudInit.writeFiles` shipped kubectl-only first. See `docs/guides/machine-guest-exec.md`'s "Related, smaller API-only endpoints" section.
- [x] Live resource usage (first cut): `status.resourceUsage` (`cpuPercent`/`memoryBytes`/`diskReadBytes`/`diskWriteBytes`) is FluxVM's own real, cgroup-derived `GET /v1/vms/{id}/stats`, refreshed every reconcile tick -- backend-agnostic, like `spec.resources.limits` (whose enforcement this reports against) rather than the QEMU-only hotplug. Best-effort: a read failure (e.g. a FluxVM predating this endpoint) never blocks the rest of reconcile, `status.resourceUsage` just stays at its last known value. The dashboard shows it inline next to each Machine's CPU/Memory columns. A point-in-time snapshot, not a time series -- for history, use Prometheus/external monitoring, not this field. See `docs/guides/machine-resource-limits.md`'s "Watching live usage" section.
- [x] Machine logs (first cut): a **Logs** button in `kairon-ui`'s dashboard streams a Machine's real captured serial console output, with a live-tailing **Follow** toggle -- Kairon's `kubectl logs`/`-f` equivalent. Wraps FluxVM's own `GET /v1/vms/{id}/logs` (`?follow=true` never terminates on FluxVM's own side until the client disconnects) -- relayed as a plain chunked `text/plain` HTTP stream through `internal/consoleproxy`/`kairon-ui`, not a WebSocket, since a unidirectional text tail doesn't need framing. Bounded entirely by request cancellation (closing the browser tab propagates all the way to FluxVM's own stream), no server-side timeout. Same any-authenticated-operator authorization as the VNC/text console (not admin-only like exec/file access) -- reading console output is closer in sensitivity to viewing a display than to running code. Only requires a runtime to have existed, not `Running` specifically -- works for a `Paused` Machine too. Caught and fixed a real, separate latent bug along the way: `internal/uiapi`'s `statusRecorder` (the `withMetrics` wrapper) didn't implement `http.Flusher`, which would have silently broken this feature's incremental streaming whenever `ui.metrics` is enabled -- Go doesn't promote an embedded interface's *other* optional interfaces just because the concrete value underneath happens to implement them. See `docs/guides/machine-logs.md`.
- [x] VM-state snapshot/restore (first cut, API-only): `POST /api/v1/machines/{ns}/{name}/vm-snapshot` and `.../vm-restore-snapshot` wrap FluxVM's own real `POST /v1/vms/{id}/snapshot`/`start-from-snapshot`/`stop` -- a full hypervisor-level checkpoint (RAM, CPU, device state via QEMU's real `savevm` or a Cloud Hypervisor snapshot) of a Machine's *running* state, restored back into the same Machine in place, completely distinct from `MachineSnapshot`'s disk-content-only CSI snapshot above. Requires orchestrating FluxVM's own stop -> start-from-snapshot sequence on restore (`internal/fluxvm/hibernate.go`'s `RestoreSnapshot`) rather than calling `start-from-snapshot` directly, since that route silently ignores the requested tag entirely when the VM is already Running -- a real trap discovered by reading `start_impl` itself, not documented anywhere in FluxVM's own API docs. `internal/agent`'s reconcile loop gained a matching self-healing branch: if a restore's stop succeeds but its start-from-snapshot step fails (including this process crashing in between), the Machine is left FluxVM-stopped with its disk/record intact, and the next reconcile tick notices the mismatch against `spec.powerState` and starts it back up from its last-known-good disk state -- a safety net, not a second automatic restore attempt. Firecracker doesn't support this at all; FluxVM's own clear error surfaces unmodified. Admin-only, same posture as guest exec/file access -- no guest-agent dependency of any kind, since this operates entirely at the QEMU/Cloud Hypervisor level. No dashboard button, no snapshot listing/deletion yet. See SECURITY.md's "VM-state snapshot/restore" section and `docs/guides/machine-vm-state-snapshot.md`.
- [x] Halted power state (`spec.powerState: Halted`, first cut): a general-purpose "power off, keep the FluxVM record" primitive, initially considered and declined earlier in this same audit (see the analysis this bullet used to carry, preserved in git history) because FluxVM's `start` reuses its own *last-applied* VM config as-is on resume rather than re-deriving it from the current Machine spec the way `Stopped` -> `Running` does -- a real spec-drift caveat, not a hidden bug. Revisited and shipped anyway: that caveat is the same, already-accepted "read only once, editing is a silent no-op" convention `spec.cloudInit`/`spec.network.forwards` carry for an already-running Machine, not a novel anti-pattern for this codebase, and the underlying `internal/fluxvm.Client.Stop`/`.Start` (renamed from the previously-unexported `stop`) were already fully built for `RestoreSnapshot`'s own orchestration. `ensureHalted` (`internal/agent/hibernate.go`) calls FluxVM's real `POST /v1/vms/{id}/stop` -- VMM process terminated, guest RAM lost, FluxVM's own record/disk kept -- and resume (`spec.powerState: Running`, `kaironctl halt`/dashboard's own **Halt**/**Resume** buttons) reuses the exact same reconcile branch that already handled `RestoreSnapshot`'s own crash-recovery self-heal, since both leave FluxVM reporting the runtime Stopped-with-record-kept. `status.phase` reports `Halted`, deliberately distinct from `Stopped`'s own phase string, and both `countAssigned` (scheduler capacity) and `machineCountsTowardQuota` (`MachineQuota`) now treat `Halted` the same way they already treat `Stopped` -- genuinely freed, not still resident. Only a single-hop resume to `Running` is handled (Halted -> Paused isn't special-cased, go through Running first, the same convention `Paused` itself already establishes). See `docs/guides/machine-halt.md`.
- [x] Backend-agnostic guest exec over the vsock agent (first cut, API-only): `POST /api/v1/machines/{ns}/{name}/agent-exec` (body `{"command", "timeoutSeconds"}`) wraps a previously-unwrapped FluxVM route, `POST /v1/vms/{id}/agent`, discovered while investigating FluxVM's own agent-sandbox routes for a separate audit. Genuinely distinct from "Guest exec" above despite the near-identical name and shape: that one goes over `qemu-guest-agent`'s virtio-serial channel (QEMU-only in practice); this one goes over FluxVM's own bespoke vsock guest agent -- the exact channel `spec.guestAgent.console` (guest file access, text console) already uses -- so it's the only guest-exec path that works on Cloud Hypervisor, Firecracker, or a FluxVm-backend sandbox. `internal/fluxvm.Client.AgentExec` (`internal/fluxvm/agentfile.go`) added alongside the existing `AgentPutFile`/`AgentGetFile`; `internal/consoleproxy`/`internal/uiapi` wiring and authorization (admin-only, same as guest file access) mirror those exactly. See SECURITY.md's "Guest file access" section and `docs/guides/machine-guest-agent-files.md`.

- [x] Sandboxes (`spec.sandbox`, first cut, admin-only, API-only): full support for FluxVM's own agent-sandbox track -- a lightweight, fast-boot in-tree hypervisor (`BackendKind::FluxVm`) for short-lived, ephemeral workloads (e.g. an AI agent's own generated code), distinct from the qemu/cloud-hypervisor/firecracker backends every other Machine uses. `kairon-node` calls FluxVM's real `POST /v1/sandboxes` (`internal/fluxvm.Client.CreateSandboxForMachine`) instead of `POST /v1/vms` when `spec.sandbox` is set; `CreateWithVFIO`'s own Machine-to-FluxVM-request mapping was extracted into a shared `buildCreateRequest` helper so both creation paths can never drift apart on what a spec field maps to. A `spec.sandbox.templateName` boots from a pre-built template instead (`internal/fluxvm.Client.ListTemplates`/`BuildTemplate`, wrapping FluxVM's own `GET`/`POST /v1/templates`, node-scoped admin API) -- FluxVM loads the template's own pre-baked image/resources/kernel and ignores `spec.image`/`spec.resources` entirely when this is set, so Kairon skips its own boot-disk resolution for a template-based sandbox rather than demanding a `spec.image.path` that would just be ignored. `spec.deviceClaims` is refused outright at create time -- the sandbox hypervisor has no VFIO passthrough. `ANY /api/v1/machines/{ns}/{name}/sandbox-http/{port}/{path}` relays an arbitrary HTTP request straight into the sandbox's own guest HTTP server (FluxVM's own `sandbox_http_proxy` route) -- the broadest capability shipped this session, admin-gated and deliberately not tied to `spec.guestAgent.console` since it dials the guest's network IP directly rather than the vsock agent. `GET /api/v1/nodes/{node}/sandboxes` lists real per-node FluxVM state (any authenticated operator, read-only). FluxVM's own `fs/read`/`fs/write`/`process` sandbox routes were deliberately **not** separately wrapped -- confirmed by reading FluxVM's own source that they're thin aliases over the exact same `get_file`/`put_file`/`exec` manager methods Kairon's existing guest file access/agent-exec routes already call by runtime ID, so no new code was needed for those. No dashboard yet. See SECURITY.md's "Sandboxes" section and `docs/guides/machine-sandboxes.md`.

- [x] Image catalog (`spec.image.catalogName`, first cut, admin-only for mutations, API-only): full support for FluxVM's own node-local image catalog -- a named, checksummed (mandatory SHA-256, optional Ed25519 signature) alias any `CreateVmRequest.image` field (including `spec.image.catalogName`) can reference interchangeably with a raw path. `internal/fluxvm.Client` gained `ListCatalog`/`AddCatalogEntry`/`RemoveCatalogEntry`/`RenameCatalogEntry`/`CloneCatalogEntry`/`ExportCatalogEntry`/`SetCatalogReadOnly`/`CleanCatalogDownloads`, one per FluxVM route (`internal/fluxvm/catalog.go`); node-scoped admin REST endpoints under `/api/v1/nodes/{node}/catalog/...` relay through `internal/consoleproxy` the same way sandbox templates do -- listing is any-authenticated-operator (read-only visibility), every mutation is admin-only (a real image download/checksum pass is a real resource cost, the same reasoning template building already established). `spec.image.catalogName` is exempt from `--image-root` path fencing entirely, and reconcile skips its own boot-disk resolution when set -- it was never a filesystem path to begin with, the same treatment `spec.image.source`-resolved paths and sandbox templates already get. Kairon never generates or verifies catalog signatures itself; `fluxvm catalog sign` (out of band, on the node) is the only way an entry gets one. See SECURITY.md's "Image catalog" section and `docs/guides/machine-image-catalog.md`.

- [x] Egress check (first cut, admin-only, API-only): `POST /api/v1/nodes/{node}/egress-check` (body `{"host"}`) wraps FluxVM's own stateless `POST /v1/egress/check` -- a diagnostic against a node's static `[sandbox]` egress allowlist/credential-vault config, no runtime-mutable resource to manage (the allowlist is read from FluxVM's own config file at daemon startup). `internal/fluxvm.Client.EgressCheck` (`internal/fluxvm/egress.go`) relays FluxVM's real response type faithfully; `internal/uiapi.handleEgressCheck` deliberately does not, discarding FluxVM's own `inject_authorization` field (a real credential-vault secret value, not a boolean) and reporting only whether one would be injected (`wouldInjectCredential`) -- covered by a dedicated regression test (`TestHandleEgressCheckRedactsCredential`) since a real secret leaking into an API response is exactly the kind of bug that needs a test that fails loudly, not just careful code. See SECURITY.md's "Egress check" section and `docs/guides/machine-sandboxes.md`'s "Checking egress policy" section.

- [x] Warm pools (first cut, admin-only, API-only): full support for FluxVM's own node-local warm-VM pools -- `size` VMs pre-booted and kept `Paused`, ready for an instant claim (resume, not a cold create). `internal/fluxvm.Client` gains `CreatePool`/`ListPools`/`GetPool`/`DeletePool`/`ClaimFromPool` (`internal/fluxvm/pool.go`), one per FluxVM route; node-scoped admin REST endpoints under `/api/v1/nodes/{node}/pools/...` relay through `internal/consoleproxy`, reusing catalog's own `requireCatalogAdmin` gate for every mutation (create/delete/claim all commit or destroy real node resources) while listing stays any-authenticated-operator. Deliberately **not** a new Kairon CRD with its own reconcile loop -- FluxVM already ships a competing `MicroVMPool` CRD/node-local controller (`fluxvm-microvm`) reconciling against this exact same `/v1/pools` state, discovered while researching this feature; a second Kairon-side controller converging its own desired pool state would risk two controllers fighting over one FluxVM resource with no coordination between them. A claimed VM is real FluxVM state, not automatically wired into a Kairon `Machine` object -- documented as a real, open gap (a manual follow-up an operator does themselves), not a hidden assumption. `DeletePool` destroys every member VM the pool currently holds, claimed or not -- surfaced verbatim from FluxVM's own real behavior, not softened. See SECURITY.md's "Warm pools" section and `docs/guides/machine-sandboxes.md`'s "Warm pools" section.

This closes out the "cluster-level resource domains" investigation (pools, image catalog, templates, sandboxes, egress) that motivated the last several features shipped this session -- every one of the five turned out to be real, implemented, per-node FluxVM state (never a genuinely cluster-shared resource despite some reading that way from their names), and each is now wrapped: sandboxes+templates+HTTP proxy, the image catalog, the egress check, and warm pools.

- [x] Runtime diagnostics (first cut, API-only): four more previously-unwrapped FluxVM routes closed as part of a fuller route audit of `crates/fluxvm-api`. `GET /api/v1/nodes/{node}/capabilities` wraps FluxVM's own `GET /v1/runtime/capabilities` -- a real, per-node, per-backend manifest of migration/snapshot support; exposed as a cross-checkable diagnostic, not (yet) wired to replace Kairon's own existing hardcoded backend-support checks (`spec.resources.numaNode/cpuSet/hugepages` requiring qemu, Firecracker refusing VM-state snapshot), both of which were independently verified against FluxVM's own source already and remain correct. `GET /api/v1/machines/{ns}/{name}/pressure` and `.../cpuset` wrap FluxVM's own `GET /v1/vms/{id}/pressure`/`/cpuset` -- real PSI (Pressure Stall Information) and effective host-CPU-pinning ground truth, any-authenticated-operator like every other read-only Machine diagnostic. `POST .../freeze`/`.../thaw` and `GET .../frozen` wrap FluxVM's own cgroup-v2-freezer-backed `/v1/vms/{id}/freeze`/`/thaw`/`/frozen` -- a genuinely different mechanism from `spec.powerState: Paused` (QMP-level, guest-CPU-only, hypervisor-cooperative): freeze halts an entire cgroup (the VMM process and every helper thread in it) at the kernel scheduler level, working even when a VM's own control channel is unresponsive -- the tool for a hung VM, not a routine pause/resume replacement. Freeze/thaw are admin-only (comparably disruptive to guest exec/VM-state restore); capabilities/pressure/cpuset/frozen-status are all any-operator read-only. See SECURITY.md's "Runtime diagnostics" section and `docs/guides/machine-diagnostics.md`.

- [x] Network observability: effective policy, stats, flows, drop-reasons (first cut, any-operator, API-only): four more previously-unwrapped FluxVM routes closed as part of the same route audit that added runtime diagnostics above, plus wiring up `fluxvm.Client.GetVMNetworkEffective` -- a method that already existed in `internal/fluxvm/network.go` with zero callers anywhere in this codebase until now. `GET /api/v1/machines/{ns}/{name}/network-effective` wraps FluxVM's own `GET /v1/vms/{id}/network/effective` -- the Machine's fully-resolved policy *after* `NetworkSecurityGroup`/label merging, what's actually enforced right now rather than what was last sent. `.../network-drop-reasons`, `.../network-flows`, `.../network-stats` wrap FluxVM's own real eBPF-dataplane-derived diagnostics -- `network-drop-reasons` in particular is the single most direct answer to "why is my policy blocking traffic I expected to allow." All four are raw JSON passthroughs (`internal/uiapi/network_observability.go`'s own `relayRawGET`), since FluxVM's own handlers return a dynamic `serde_json::Value` here, not a fixed struct. Deliberately narrow scope: FluxVM's own Cilium-style network dataplane exposes a much larger surface (CNP/identity/ipcache management, Hubble flow observability, per-service health/stats/telemetry/conntrack-state-transfer, L7 Envoy contracts) discovered during this same audit -- not wrapped, since most of it is internal node-to-node coordination machinery rather than an operator-facing capability, or (Hubble) substantial enough to need its own dedicated design pass. See SECURITY.md's "Network observability" section and `docs/guides/network-policy.md`'s "Troubleshooting" section.

- [x] SR-IOV NIC passthrough via DRA/VFIO reuse (first cut): confirmed the existing GPU/VFIO passthrough mechanism (`spec.deviceClaims[]` -> `ResourceClaim` -> PCI BDF -> FluxVM's own `vfio_devices`) is deliberately device-type-agnostic -- it never inspects what kind of PCI device a BDF names, and FluxVM's own `CreateVmRequest.vfio_devices` already loops over multiple BDFs with no per-device-type logic. An SR-IOV Virtual Function, once created and bound to `vfio-pci` on the host (operator/host setup, out of band, the same posture GPU passthrough already has -- Kairon manages neither), passes through via this exact same path with **zero new allocation code**. Multus, the standard Kubernetes SR-IOV integration path, doesn't apply to Kairon at all -- its whole mechanism wires extra interfaces into a Pod's own network namespace, and a Machine has no Pod for it to attach to. Real, separate gap closed alongside this: `status.guestIP` (`fluxvm.BestGuestIP`) previously picked the first IPv4 address across every guest-reported interface with no concept of "primary" vs. "passthrough" -- a VF's own DHCP lease could nondeterministically clobber it. `BestGuestIPWithPrimary` now prefers the interface matching FluxVM's own known primary-NIC MAC (`Record.Request.Network.MAC`) when one is known, since a passthrough VF always keeps its own real hardware MAC, never one FluxVM assigns. See `docs/guides/machine-sriov.md`.

- [x] Real CPU pinning (`spec.resources.cpuPinning`, first cut, qemu-only): closes the "not exclusive host-core pinning" gap `machine-cpu-numa.md`'s own `cpuSet` field has always documented -- FluxVM already had the real primitive (`cgroup v2 cpuset.cpus`, its own `ResourcePatch.cpuset_cpus`), what was missing was entirely Kairon-side: `internal/scheduler` gained a real capacity model. A new operator-asserted node label, `kairon.zyvor.dev/pinnable-cpus` (Linux `cpuset.cpus`-list syntax, e.g. `"2-15"`), is the fail-closed source of truth for which host cores are safe to exclusively hand out -- the same "operator asserts a fact Kairon can't otherwise know" shape `storage-domain`/`network-domain`/`vfio-devices` labels already use. `eligible` now hard-filters out any candidate node without enough free labeled cores for a `cpuPinning` Machine (free = the label minus every other already-assigned Machine's own `spec.resources.allocatedCpuSet`); `scheduler.AllocateCPUSet` deterministically picks the exact core numbers in the same scheduling pass, and `internal/controller` patches `spec.nodeName` and `spec.resources.allocatedCpuSet` together in one call, so the two decisions can never land separately. `internal/agent/resourcelimits.go` merges the allocated set into the same `POST /v1/vms/{id}/resources` call `spec.resources.limits` already uses, rather than two separate/competing writes. Deliberately does **not** attempt to read kubelet's own internal, undocumented `cpu_manager_state` file to auto-avoid cores already claimed by real Kubernetes Pods -- a known, fragile, version-dependent, unsupported community pattern; the operator-asserted label already carries that responsibility instead, a real, explicitly-documented limit, not a hidden gap. Not NUMA-topology-aware in this first cut (core selection doesn't cross-validate against `spec.resources.numaNode` even when both are set). See `docs/guides/machine-cpu-pinning.md`.
- [x] Generic third-party CSI client (`node.thirdPartyCSIDrivers`, first cut): closes the "any CSI driver already in your cluster" gap by giving `kairon-node` a second CSI-client path (`internal/agent/csi_thirdparty.go`) alongside its own iSCSI driver's existing one, dialing an **operator-configured allowlist** of `driverName=/socket/path` entries rather than reimplementing kubelet's own `plugins_registry/` registration listener -- the same fail-closed-allowlist shape `KAIRON_VFIO_ALLOWLIST` already has, applied to a new domain. `resolveCSIVolume` now branches on whether a PV's `spec.csi.driver` is in the allowlist; the third-party path builds `NodeStageVolumeRequest`/`NodePublishVolumeRequest` from the PV's own fields directly, using a Kairon-synthesized staging/publish path keyed by the Machine's own deterministic `RuntimeName()` in place of kubelet's Pod-UID convention (no Pod exists here for one to come from -- a named, real compatibility risk for a driver that specifically depends on that path shape). Deliberately **never resolves a Secret** (`Secrets` stays nil in both requests), the identical tradeoff Kairon's own iSCSI driver already made, to avoid granting `kairon-node` cluster-wide Secret-read RBAC -- this is the reason first-cut compatibility is narrow rather than broad. New `status.volumeDriver` field (empty = Kairon's own driver, for backward compatibility) records which driver staged a Machine's volume so teardown routes correctly even after the PV/PVC is already gone. Scoped explicitly to `attachRequired: false` drivers (no `ControllerPublishVolume`/attach plumbing exists) -- Ceph-CSI/RBD is the only conceptually-validated reference case; cloud block-storage drivers (EBS-CSI, PD-CSI, Azure Disk CSI) won't work. No live third-party driver was available in this repo's own test environment to validate end-to-end; request-building and allowlist-fail-closed behavior have real unit coverage against a fake gRPC CSI server instead. Helm wiring mounts the node's entire `/var/lib/kubelet/plugins` directory read-only into `kairon-node` at its identical host path (rather than templating a mount per dynamically-keyed driver), broader than the sockets actually dialed but simpler and still read-only. See `docs/guides/machine-storage-thirdparty-csi.md`.
- [x] Independent kairon-node liveness signal for fencing (`node.livenessLease.enabled`, opt-in, off by default): closes a real gap the fencing guide already documented honestly -- `NodeUnreachable` detection was Node-`Ready`-only, so a kubelet flap (a brief network blip, an apiserver hiccup) could look identical to a truly dead node, and `kaironctl fence` had no independent way to tell them apart before clearing a Machine's runtime state. New `internal/nodeliveness` package (deliberately *not* a reuse of `internal/leaderelection`, which solves mutual exclusion across contending replicas -- a per-node liveness Lease has no contention at all, since only that node's own `kairon-node` ever legitimately writes `kairon-node-<nodeName>`'s Lease, so the full acquire/takeover state machine would be unused complexity here): `Agent.Run` renews its own `coordination.k8s.io/v1` Lease once per reconcile tick when `LivenessLeaseNamespace` is set (env `KAIRON_NODE_NAMESPACE`, wired via the Downward API, the same opt-in-via-namespace convention `kairon-controller`'s own leader election already established). `kaironctl fence --liveness-lease-namespace ...` cross-checks that Lease before proceeding: a still-fresh renewal despite `NodeUnreachable=True` refuses the fence outright (`--force-ignore-liveness` overrides), rather than risk abandoning a still-live VM whose `kairon-node` may in fact still be actively managing it. Deliberately an **additional** safety gate layered on the existing check, never a replacement -- omitting the new flag (the default) skips it entirely, exactly `fence`'s behavior before this existed, and a missing/unreadable Lease also fails open to that same prior behavior rather than blocking on infrastructure the check itself depends on. Does not change `NodeUnreachable` detection itself -- a wedged `kairon-node` on an otherwise-`Ready` node still isn't caught by anything in this codebase, a real, explicitly-documented limit, not a hidden one. See `docs/guides/machine-fencing.md`.
- [x] OIDC group-to-admin mapping (`ui.oidc.adminGroups`, opt-in, empty by default): closes the "OIDC never grants admin" gap README.md's own Production-gaps section documented honestly -- admin capability used to be exclusively a static `ui.auth.users[].admin: true` property, with no way for an operator relying entirely on SSO to grant admin without also maintaining a parallel `ui.auth.users` account. New `sessionPayload.Groups` (only ever set by the OIDC callback, from `ui.oidc.groupsClaim`, default `"groups"`) is recorded on the session token at login time and threaded through `withAuth`/context the same way `usernameHolderKey` already is (`groupsHolderKey`, mirroring it exactly). New `Server.isAdminIdentity` replaces the `findUser(username).IsAdmin` two-liner duplicated across 10 admin-gated routes: true for a static admin account (unchanged), or for an OIDC session whose recorded groups currently intersect `ui.oidc.adminGroups` -- **re-checked fresh against current config on every single call**, deliberately not baked into a computed `true`/`false` bit inside the signed token itself, so an operator changing `adminGroups` takes effect on an already-issued session's very next request without the caller needing to log in again, the same freshness property `findUser`'s own live `s.Users` lookup already has for the static path. The IdP-side group membership itself is a different story -- only as fresh as the caller's last login (the ID token is only ever read at sign-in time), a real, explicitly-documented asymmetry, not a hidden one. Deliberately does not special-case the legacy shared-token caller (`username == ""`) inside `isAdminIdentity` itself -- each of the 10 call sites keeps its own pre-existing (and, confirmed during this change, already slightly inconsistent across routes) handling of that case unchanged, rather than silently unifying behavior nobody asked to change. 13 new tests (a full fake-OIDC-provider round trip proving admin-group grant/deny/config-change-takes-effect-immediately, plus an end-to-end proof an OIDC-admin session can actually call `POST /api/v1/users/{username}/password`). See `docs/guides/kairon-ui-oidc.md`.
- [x] Opt-in Machine creation on warm-pool claim (`POST .../claim`'s `createMachine`/`namespace`/`machineName`, off by default): closes the "claimed VM sits outside `MachineQuota`/the webhook until an operator explicitly creates one" gap README.md's Production-gaps section already documented honestly. The real coupling problem this had to solve: kairon-node only ever *adopts* an existing FluxVM runtime by looking it up via `RuntimeName()` (`"kairon-<namespace>-<name>"`) when a Machine has no `status.runtimeID` yet (`Agent.current`'s existing fallback) -- so a naively auto-created Machine pointing at a claimed VM with an unrelated FluxVM-side name would make kairon-node boot a *second*, duplicate VM instead of adopting the one just claimed. `handleClaimPool` now forces the claim's own FluxVM-side name to the exact `RuntimeName()` a `Machine{Name: machineName, Namespace: namespace}` would compute, overriding any `name` override also set, so kairon-node's existing adoption path — unmodified — picks up precisely that runtime on its next tick. The new Machine's `spec.image`/`spec.resources`/`spec.runtime.backend` come from the **pool's own template**, fetched fresh via a `GET .../pools/{name}` call at claim time (the same spec every member was actually booted from) rather than trying to reverse-engineer a spec from the claimed record's own FluxVM fields, which don't echo back everything a Machine spec needs (the image path isn't present on a claim response at all). First cut: only image/cpu/memory/backend are carried over, not VFIO/NUMA/cloud-init or every other `CreateVmRequest` field a template might set. `machineName` is required when `createMachine` is set (fails closed with 400 rather than guessing a name); a Machine-creation failure after a successful claim reports both (`"vm"` populated, `"machine": null`, a `"machineError"` message) rather than hiding that the claim itself already succeeded. Deliberately opt-in, not the new default -- warm pools exist for fast, low-ceremony ephemeral VMs, and a full Machine object drags in finalizer-gated deletion/quota accounting/webhook validation not every caller wants; omitting `createMachine` is byte-for-byte the prior behavior. 3 new tests (forced-name + created-Machine-from-template round trip, the `machineName`-required validation, and a regression test proving the no-`createMachine` path is byte-for-byte unchanged). See `docs/guides/machine-sandboxes.md`'s "Warm pools" section.
- [x] kairon-node self-checks `MachineQuota` on hotplug resize, independent of `webhook.enabled` (default on, fail open): closes the other half of the gap README.md's Production-gaps section documented honestly -- the admission webhook's own `MachineQuota` `UPDATE` check already caught a resize-past-quota, but only when `webhook.enabled` is set (off by default); with it off, a resize past quota went uncaught anywhere, since kairon-node's own hotplug reconciliation had no cluster-wide quota visibility of its own. `internal/controller/quota.go`'s admission primitives (`QuotaTracker`/`BuildQuotaTrackers`/`AdmitQuotaResize`/`MachineFootprint`/`MachineCountsTowardQuota`) are now exported, plus a new shared `QuotaTrackersForNamespace` extracted from what was previously the webhook's own private helper -- so `internal/agent`'s new `quotaBlocksResize` (called from `reconcileHotplug` right before a live grow) reuses the *exact* same admission logic the webhook uses, rather than a second implementation that could quietly drift from it. One real correctness subtlety, caught before ever running a test: the webhook runs *before* a resize is persisted, so its own tracker sees every Machine (including the one resizing) at its OLD footprint, matching `AdmitQuotaResize`'s `(old, new)` delta contract -- but kairon-node only ever observes a Machine *after* the resize is already persisted to `spec.resources`, so its own fresh `QuotaTrackersForNamespace` call already counts the Machine at its TARGET footprint, not what's actually been hotplugged so far. Passing `(applied, target)` as `(old, new)` here would double-count the growth; the fix passes `(target, target)`, which collapses `AdmitQuotaResize`'s `used - old + new` arithmetic to exactly `used`, the correct "is the total, already including this Machine at target, over the limit" check. A blocked resize is logged and silently reset to the currently-applied vCPUs/memory (the same "shrink is ignored" no-op shape hotplug already uses for an explicit spec decrease), not surfaced as a Machine error -- the next successful reconcile tick (once quota frees up, or the spec is lowered) retries automatically. Effectively on by default with no new Helm flag, but fails open: any `QuotaTrackersForNamespace` lookup error -- most commonly a 403 from a kairon-node ServiceAccount not yet granted the new read-only `machinequotas`/`machinequotas/status` RBAC this needs (a binary upgraded ahead of its chart) -- logs a warning and proceeds with the resize exactly as before this existed, rather than newly blocking a resize on account of infrastructure the check itself depends on. 4 new tests (blocked-by-quota, allowed-within-quota, no-MachineQuota-in-namespace-is-a-no-op, fails-open-on-lookup-error). See README.md's Production-gaps section.
- [x] Windows 11: wire `spec.security.secureBoot`/`.tpm` through to FluxVM's real UEFI/vTPM support (backend-scoped): closes the follow-up half of the earlier "Windows guest support" gap this file already tracked -- these fields were refused outright at `CreateWithVFIO` because no FluxVM backend implemented UEFI/OVMF or a vTPM device at the time; FluxVM has since shipped real support for both (`ff145b4` QEMU Secure Boot + vTPM, `e2bd218` extending vTPM to Cloud Hypervisor), so the refusal itself was now a stale gap rather than an honest upstream limit. `internal/fluxvm.CreateRequest` gains `secure_boot`/`tpm` fields passed straight through from `m.Spec.Security`; `buildCreateRequest` enforces the identical backend restriction FluxVM's own scheduler enforces server-side (`secureBoot`: qemu-only, permanently -- Cloud Hypervisor's own `--firmware` has no documented separate vars store to enroll Secure Boot keys into; `tpm`: qemu or cloud-hypervisor, both dial a real `swtpm` socket) rather than relaying FluxVM's own 400 with no Kairon-side context, mirroring the existing NUMA/CPUSet/Hugepages backend check right above it. Deliberately does not add a `Machine`-spec field for an OVMF firmware/vars-template path -- that stays a FluxVM node-level operator responsibility (`Config.qemu_ovmf_code`/`.qemu_ovmf_vars_template`), and setting `secureBoot: true` against a node that hasn't configured a vars template fails closed with FluxVM's own clear error rather than a silent no-op. Honestly not yet re-verified end-to-end from the Kairon side specifically: FluxVM's own `e2bd218` already proved real `qemu-system-x86_64`/`cloud-hypervisor`/`swtpm` argv construction and an actual OVMF boot directly against its API on a remote host, but no `kairon-node` build has made a real `POST /v1/vms` call with these fields set against a live FluxVM instance in this repo's own CI -- the Go-side request-mapping and backend-scoping logic has full unit coverage instead. See `docs/guides/machine-windows-guests.md`.
- [x] Trusted forwarded-header rate-limit keying (`ui.rateLimit.trustedProxyHeader`/`.trustedProxyCidrs`, opt-in, both empty by default): closes the "rate limiting collapses behind a load balancer" gap README.md's Production-gaps section documented honestly -- `kairon-ui`'s per-remote-address rate limiter (`-rate-limit-rps`/`-rate-limit-burst`, on by default) was keyed exclusively by `r.RemoteAddr`, so every real client behind a `Service`/`Ingress`/load balancer with no forwarded-header handling collapsed into that one proxy's address, letting one noisy client throttle everyone. New `Server.clientIP` reads the configured header (e.g. `X-Forwarded-For`, taking its leftmost entry -- the original client, by that header's own append-on-the-right convention) instead of the TCP peer, but only when that direct peer itself falls inside `TrustedProxyCIDRs` -- the load-bearing check that keeps this from becoming a spoofing vector: without it, any client talking to `kairon-ui` directly could set the header itself to dodge its own limit or collide with an unrelated client's bucket. `-trusted-proxy-header`/`-trusted-proxy-cidrs` are all-or-nothing at startup (fails closed on a half-set pair, same posture OIDC's three flags already use) and every `-trusted-proxy-cidrs` entry is validated as a real CIDR before the process ever serves traffic. Both empty (the default) is byte-for-byte prior behavior. 5 new tests covering the untrusted-peer-ignores-header case, the trusted-peer-uses-header case, missing/malformed-header fallback, and a handler-level test proving two distinct real clients sharing one trusted load-balancer address get independent buckets. See SECURITY.md's rate-limiting section.
- [x] Per-route breakdown for `kairon-ui`'s own HTTP request metrics (`kairon_ui_request_duration_seconds`, no new Helm flag): closes the "method/status-class only, no path" gap `docs/guides/observability.md`'s own Real-limits section documented honestly. The obvious naive fix -- labeling by the raw request path -- was exactly what that gap was avoiding in the first place: dynamic `{namespace}/{name}` segments would multiply label combinations unbounded as Machines come and go, the precise Prometheus cardinality-explosion footgun this metric was deliberately designed around from the start. The actual fix needed no new tracking machinery at all: Go 1.22's `http.ServeMux.Handler(r)` already reports which *registered pattern* a request matched (e.g. `"GET /api/v1/machines/{namespace}/{name}"`), separately from the request's own raw path -- exactly the bounded-cardinality label this needed, sitting unused in the stdlib the whole time. New `internal/uiapi.routePattern(top, api, r)` resolves it in two steps since `Handler()`'s own dispatch is per-mux, not recursive: a request that top's own dispatch reports as "/api/v1/" (its literal registered prefix pattern for the entire authenticated API sub-mux) gets a second lookup against `api` itself for the actual matched sub-route; everything else (`/healthz`, `/metrics`, the unauthenticated auth/oidc/console routes, the `/` SPA catch-all) resolves directly off `top`. A sub-lookup that finds nothing (a genuine 404 inside `/api/v1/`) reports `route="unmatched"` rather than being misreported as the outer `/api/v1/` prefix itself, which would conflate two different things under one label value. The leading `"METHOD "` Go's `ServeMux` embeds in a method-scoped pattern's own string is stripped before use, since `method` is already tracked as its own separate label -- keeping it would just duplicate "GET" into both. 3 new/extended tests, including one proving two distinct real Machine paths sharing the same dynamic pattern collapse into a single route-labeled bucket rather than two. See `docs/guides/observability.md`.
- [x] Cap every `kairon-ui` JSON request body size (`decodeJSON`, opt-out only in the sense that it can't be disabled -- this is a plain correctness/hardening fix, not a new capability to gate): closes a real gap found while auditing `docs/guides/machine-guest-agent-files.md`'s own honestly-documented "Writes have no such cap enforced on Kairon's side" limit -- confirmed, on inspection, to actually be true of *every* JSON-accepting route in `internal/uiapi`, not just that one: nothing anywhere in the package (not `decodeJSON`, not the `http.Server` kairon-ui constructs) ever wrapped a request body in `http.MaxBytesReader`, so a caller (even a legitimately authenticated one) could send an arbitrarily large body and have `json.Decoder` buffer all of it into memory before `DisallowUnknownFields` ever got a chance to reject it. `decodeJSON` now defaults to a 1MiB cap (`defaultMaxRequestBodyBytes`) -- comfortably above every ordinary request this API accepts (Machine/MachineSet specs, exec commands, pool/catalog requests) -- and a new `decodeJSONWithLimit` lets a route override it; `handleAgentPutFile` is the one real exception, raised to 6MiB (`maxAgentFileBodyBytes`) to match the read side's own already-documented ~3MB-of-real-content ceiling once base64-encoded. An oversized body is now rejected with a plain 400 (`http.MaxBytesError`, detected via `errors.As`) rather than accepted and buffered without limit. All 25 existing `decodeJSON` call sites across the package were mechanically updated to the new signature (the function itself is unexported, so this is an internal-only change with no wire-format or Helm-chart impact); 5 new tests cover the default limit rejecting/accepting, the explicit override actually taking effect (not silently falling back to the smaller default), and a full HTTP-level proof that a real oversized guest-file write gets a clean 400 on this exact route. See README.md's Production-gaps section and `docs/guides/machine-guest-agent-files.md`.
- [x] Readiness-gated `MachineSet` `RollingUpdate` (no new Helm flag, no spec change): closes the "paced by count, not health" gap this file's own `MachineSet` entry above, and `docs/guides/machine-sets.md`'s own "Real limits today" section, both documented honestly at the time. `stepMachineSetToward`'s `RollingUpdate` branch previously bounded deletion of outdated replicas by `len(current) + len(outdated)` -- counting a current-template replica as available capacity the instant its `Machine` object existed, before kairon-node had even scheduled it, let alone booted it to `Running`. That let the rollout tear down another old, healthy replica to "make room" for a replacement that hadn't actually come up yet, transiently dropping real available capacity below `spec.maxUnavailable`'s own intent. Fix: the bound is now `readyCurrent + len(outdated)` (`readyCurrent` = current-template replicas with `status.phase == "Running"`, already computed by `reconcileMachineSet` for `status.readyReplicas` and simply threaded one level deeper into `stepMachineSetToward`) -- a stuck-Pending replacement now correctly withholds further disruption until it either reaches `Running` or the operator intervenes, rather than the rollout barreling through every replica regardless. The "total < desired" under-provisioned check above it is deliberately untouched -- that's about how many Machine objects exist, not their readiness, and gating object *creation* on prior objects' readiness would risk a rollout wrongly stalling on its own account-for-shortfall step. 2 new tests (withholds deletion while a replacement is still Pending; resumes once it reaches Running), plus every pre-existing `stepMachineSetToward`/`reconcileMachineSet` test still passes unchanged (all used exclusively `Running` fixtures already, so `readyCurrent` there always equals `len(current)` -- no behavior change for an already-healthy fleet). See `docs/guides/machine-sets.md`.
- [x] Per-item reconcile-error visibility (`kairon_reconcile_item_errors_total{kind}`, no new Helm flag): closes a real gap found while auditing `Controller.Reconcile`/`Agent.Reconcile` against README.md's own "a reconcile error is only attributed to 'this tick failed,' not which Machine/migration inside it" claim -- the deeper truth turned out to be worse than the doc said: `kairon_reconcile_errors_total` only ever counted a whole tick returning an error outright (a `List*` call itself failing), while the far more common case -- one Machine/MachineMigration/MachineSnapshot/MachineSnapshotRestore among many failing its own reconcile step inside a `for` loop -- is caught, logged individually (with its own namespace/name, already), and status-patched *without the tick itself returning an error*, so it never incremented that counter at all. A batch of Machines all failing reconciliation simultaneously (an RBAC regression, a bad FluxVM node) would previously show `kairon_reconcile_errors_total` steady at 0 the entire time. New `kairon_reconcile_item_errors_total{kind="machine"|"migration"|"snapshot"|"snapshotrestore"}` (`internal/metrics.ObserveReconcileItemError`) now counts exactly those, called from every one of the 5 existing per-item swallowed-error sites across `internal/controller/controller.go` and `internal/agent/agent.go` -- labeled by resource *kind* only, deliberately never by name/namespace, to keep cardinality bounded the same way the just-shipped `route` label on `kairon_ui_request_duration_seconds` already is. New `KaironReconcileItemErrorRateHigh` alert (`charts/kairon/alerts.yaml`) alongside the existing `KaironReconcileErrorRateHigh`, which is left untouched -- the two counters answer genuinely different questions ("is the loop itself broken" vs. "is a specific kind of resource failing to reconcile repeatedly") and neither should be folded into the other. Still not a substitute for a real trace: the metric can say "machine reconciliation keeps failing," not which Machine -- that's still the accompanying log line's job. See README.md's "No distributed tracing" bullet and `docs/guides/observability.md`.
- [x] `MachineSnapshotRestore` no longer fails permanently on a snapshot that just isn't ready *yet*: closes a real bug found by re-reading `docs/guides/machine-snapshot-restore.md`'s own "Real limits today" section against the actual `reconcileSnapshotRestore` source, next to the identical-shaped, already-correct handling one function away in `reconcileVolumeSnapshots`. Before this, a `MachineSnapshot` that existed but was still `Pending` (or whose underlying `VolumeSnapshot` hadn't finished provisioning yet) made `reconcileSnapshotRestore` return a plain Go error -- which `Controller.Reconcile`'s own per-item handling turns into a permanent `status.phase: Failed` with no further retries, since a terminal-phase restore short-circuits to a no-op on every subsequent tick. A `MachineSnapshotRestore` created even slightly before its snapshot finished -- an entirely ordinary sequencing race, not a real error -- was stuck Failed forever, needing a manual delete-and-recreate, even though the snapshot itself went on to succeed moments later. New `parkRestorePending` (`internal/controller/restore.go`) records a `Pending` status naming exactly what it's waiting on and returns `nil`, matching the "waiting on an external condition" posture `reconcileVolumeSnapshots`'s own CSI-VolumeSnapshot-not-ready case and `finishRestoreFromPVCState`'s own PVC-not-bound case already use -- so the restore is retried automatically every reconcile tick until its dependency catches up, same as those two. `status.phase: Failed` is now reserved for what genuinely can't self-resolve: a missing/misspelled `spec.snapshotName`/`spec.targetClaimName`, an ambiguous/unknown `spec.volumeName`, or a real PVC-creation error. 2 new tests (`TestReconcileSnapshotRestoreParksPendingOnANotYetReadySnapshot`/`...VolumeSnapshot`), plus the pre-existing test asserting the old (buggy) error-returning behavior corrected to assert the new one. See `docs/guides/machine-snapshot-restore.md`'s "Real limits today" section.
- [x] `MachineSnapshotRestore` also parks `Pending` (bounded, 30s) rather than permanently `Failed` when its `spec.snapshotName` doesn't resolve to any `MachineSnapshot` object yet: the other half of the not-ready-yet-vs-doesn't-exist gap the previous entry above deliberately left open ("there's no way to tell 'will exist later' from 'will never exist'"). New `ObjectMeta.CreationTimestamp` (always set by the apiserver on a real object, zero-value only on one this process constructed itself and hasn't round-tripped through the API) resolves that ambiguity within a bounded window: `internal/controller/restore.go`'s new `missingSnapshotGracePeriod` (30s, sized like `snapshot.go`'s own `quiesceFreezeTimeout`) treats a still-missing `MachineSnapshot` as an ordinary GitOps-apply-ordering race (a `MachineSnapshotRestore` and its `MachineSnapshot` submitted together, the restore's own reconcile tick landing first) while the restore is nearly brand new, parking `Pending` and retrying every tick -- and as a genuine, permanent misconfiguration (a typo'd `spec.snapshotName`) once that window passes, returning to the prior `Failed` behavior exactly. 2 new tests (`TestReconcileSnapshotRestoreParksPendingWhenSnapshotDoesNotExistYet`/`...FailsWhenSnapshotNeverAppears`). See `docs/guides/machine-snapshot-restore.md`'s "Real limits today" section.
- [x] `NetworkSecurityGroup` deletion now fails closed on a real FluxVM delete error: `internal/agent/network.go`'s `reconcileSecurityGroup` used to discard `Flux.DeleteNetworkGroup`'s own error entirely (`_ = a.Flux.DeleteNetworkGroup(...)`) and remove the finalizer unconditionally -- unlike `Agent.cleanup`'s own Machine-deletion path, which already fails closed (keeps the finalizer, retries next tick) on an equivalent FluxVM runtime-delete failure, with its own comment explaining exactly why. A transient FluxVM-side failure (node briefly unreachable, a dropped connection) during a `NetworkSecurityGroup` delete meant the Kubernetes object vanished anyway, while the FluxVM-side security group state it was meant to remove kept existing -- orphaned, with no Kubernetes object left to retry the deletion through. `reconcileSecurityGroup` now returns an error (surfaced via the existing per-item `status.phase: Error` + `kairon_reconcile_item_errors_total` machinery, unchanged) instead of proceeding, and `fluxvm.Client.DeleteNetworkGroup` was rewritten to hand-roll its HTTP call exactly like `Client.Delete` (Machine) already does, tolerating a 404 as success -- required so a retry of an already-completed deletion (e.g. a delete that succeeded but whose subsequent finalizer-removal `Patch` then failed) doesn't deadlock the object in Kubernetes forever. 6 new tests (3 at the FluxVM-client level: succeeds / tolerates-already-deleted / propagates-real-errors; 3 at the agent level: keeps-finalizer-on-error / removes-finalizer-on-success / tolerates-already-deleted). `MachineNetworkPolicy`'s own per-Machine policy-reset-on-delete loop (`reconcileMachineNetworkPolicy`) has the same discarded-error shape and was deliberately left alone in this pass -- it fans out over every selected Machine rather than a single delete call, and distinguishing "that Machine's runtime is genuinely gone (a legitimate no-op)" from "a real reset failure" from a generic error string isn't yet possible without its own scoped follow-up; a real, explicitly-documented limit, not a hidden one. See `docs/network-fabric.md`'s "Agent behavior" section.
- [x] `MachineNetworkPolicy`'s own per-Machine policy-reset-on-delete loop now fails closed too, closing the follow-up the previous entry above deliberately left open: `internal/agent/network.go`'s `reconcileMachineNetworkPolicy` used to discard `Flux.SetVMNetworkPolicy`'s own error entirely (`_ = a.Flux.SetVMNetworkPolicy(...)`) while resetting every selected Machine to `default_allow: true`, then removed the finalizer unconditionally regardless of whether any reset actually succeeded. A genuine reset failure on even one selected Machine let the `MachineNetworkPolicy` vanish from Kubernetes anyway, while that Machine's VM kept running under the policy's now-stale restriction, with nothing left in Kubernetes to retry the reset through. The distinguishing trick the prior entry was missing: `fluxvm.Client.SetVMNetworkPolicy` was rewritten to hand-roll its HTTP call exactly like `DeleteNetworkGroup`/`Delete` (Machine) already do, tolerating a 404 as success -- so "that Machine's VM is genuinely gone" (a legitimate no-op, nothing to reset) is now cleanly distinguishable from "a real reset failure" (FluxVM unreachable, a transient error), the same way `DeleteNetworkGroup`'s 404 tolerance already separates those two cases for `NetworkSecurityGroup`. `reconcileMachineNetworkPolicy` now returns an error on a genuine reset failure instead of proceeding to remove the finalizer, retried automatically next tick (surfaced via the existing per-item `status.phase: Error` + `kairon_reconcile_item_errors_total` machinery, unchanged). 5 new tests (2 at the FluxVM-client level: tolerates-already-gone-VM / propagates-real-errors; 3 at the agent level: keeps-finalizer-on-error / removes-finalizer-on-success / tolerates-already-gone-VM). See `docs/network-fabric.md`'s "Agent behavior" section.
- [x] `kaironctl get machinesets`/`instancetypes`/`migrationpolicies` (no new Helm flag, no new architecture): closes a real, un-flagged gap found while auditing `kaironctl get`'s own resource dispatch against every namespaced `kairon.zyvor.dev` CRD that actually exists -- `machine`/`migration`/`snapshot`/`restore`/`quota`/`budget` were already supported, but `MachineSet`, `MachineInstanceType`, and `MigrationPolicy` (all three added in earlier work, all three plain Kubernetes CRDs `kaironctl` already talks to via the exact same `internal/kube.Client` it uses for everything else) had no `get` case at all -- an operator had to fall back to `kubectl get machinesets`/`kubectl get -o json` to see them, or (for `MachineInstanceType`) go read `kubectl describe` output to find `spec.resources` before it could be referenced from `spec.instanceTypeName`. Unlike the pool/catalog/sandbox/template `kaironctl` verbs ruled out earlier this session (those are FluxVM node-local HTTP-only state `kaironctl` has no client architecture for at all), these three needed nothing new: `internal/kube.Client` already had `ListMachineSetsNamespace`; `ListMachineInstanceTypesNamespace`/`ListMigrationPoliciesNamespace` were two small additions alongside their existing cluster-wide-only counterparts (`ListMachineInstanceTypes`/`ListMigrationPolicies`, used internally by `kairon-controller`'s own reconcile loop), following the identical `List<Kind>`/`List<Kind>Namespace` pairing every other resource in that file already has. `usage()`'s own resource list was also stale (missing `budgets`, which has worked since it shipped) -- corrected alongside the new entries rather than left to compound further. 2 new tests on the two new `Client` methods (matching `TestListAndPatchMachine`'s existing httptest-server shape); `cmdGet` itself has no dedicated test harness to extend (its output goes straight to `fmt.Printf`, true of every existing resource case too, not a gap introduced here). See `docs/guides/machine-sets.md`/`machine-instance-types.md`/`migration-policies.md`.
- [x] `kaironctl describe`/`delete` now take an optional resource kind, matching `get`'s own dispatch: found immediately after the entry above, since `describe`/`delete` were never audited the same way -- both took a bare `NAME` with zero resource-type argument and *always* called `GetMachine`/`DeleteMachine` regardless of what the caller actually meant, for every one of the nine kinds `get` already recognizes. This wasn't just a missing convenience: `kaironctl delete my-snapshot`, intended to delete a `MachineSnapshot` named `my-snapshot`, silently deleted an unrelated same-named `Machine` instead if one happened to exist -- a real, if narrow, misfire risk, not merely an absent feature. `resourceKindAndName` (`internal/kaironctl/kaironctl.go`) now backs both verbs, resolving `KIND` purely by positional-argument count (1 arg = `NAME` alone, defaulting to `machine` and preserving the exact prior calling convention byte-for-byte; 2 args = `KIND NAME`) rather than by pattern-matching `NAME`'s own text against the alias list -- deliberately, so a resource literally named e.g. `snapshot` is never misrouted by its own name. `internal/kube.Client` gained the 14 `Get`/`Delete` methods this needed across the six kinds that had neither (`MachineSnapshotRestore`/`MachineQuota`/`MachineDisruptionBudget`/`MachineSet`/`MachineInstanceType`/`MigrationPolicy`) plus the two that had `Get` but no `Delete` (`MachineMigration`/`MachineSnapshot`), each a one-line body following `GetMachine`/`DeleteMachine`'s own exact request-building pattern -- no new architecture, same as the `get` fix before it. `usage()` updated to `describe [RESOURCE] NAME`/`delete [RESOURCE] NAME`. New tests: `TestResourceKindAndName` (pure, no I/O) covers the count-based disambiguation including the same-named-as-its-own-kind edge case explicitly; `TestGetAndDeleteEveryDescribeDeleteEligibleResourceKind` exercises all 8 new `Client` methods (plus the two pre-existing `Get`s) against one fake server. See README.md's CLI section.
- [x] `kairon-ui` REST API gains read-only routes for `MachineQuota`/`MachineDisruptionBudget`/`MachineSet`/`MachineInstanceType`/`MigrationPolicy` (first cut, API-only): the same completeness gap the immediately preceding `kaironctl get`/`describe`/`delete` fixes closed for the CLI turned out to exist in the dashboard's own API too, confirmed by grepping `internal/uiapi` for any route naming these five kinds and finding none -- an operator using only kairon-ui (not kubectl or kaironctl) had no way to see quota usage or disruption-budget state at all, both first-class admission-time concerns `README.md`'s own "Guarding the fleet" section already treats as central. `GET /api/v1/quotas`/`disruption-budgets`/`machinesets`/`instancetypes`/`migration-policies` (`internal/uiapi/fleet.go`, new) each wrap the identical `List*Namespace` call `kaironctl get` already uses -- any-authenticated-operator, matching every other read-only cross-checkable resource this project exposes (runtime diagnostics, network observability). List-only, no dashboard button and no create/update/delete route in this first cut -- `kaironctl`/`kubectl` remain how any of the five actually get mutated. See README.md's Production-gaps section.
- [x] `kairon-node` refuses to delete a Machine's FluxVM runtime while a `MachineMigration` targeting it is still in flight: `internal/agent/agent.go`'s `reconcileMachine` used to route straight into `cleanup` the instant `metadata.deletionTimestamp` was set, with no check at all for whether some `MachineMigration` in the same namespace still names this Machine (`spec.machineName`) and hasn't reached a terminal phase yet. A live QEMU migration's `migrate` command is asynchronous on the hypervisor side -- once `reconcileMigration` starts one, the RAM/state transfer keeps streaming whether or not this reconcile loop is watching it -- so an operator (or automation) deleting the source Machine mid-transfer would have had `cleanup` call `Flux.Delete` on the very runtime that transfer was reading from, racing a live migration with no coordination between the two and no path into `NeedsRecovery` to name the resulting ambiguity, unlike every other ambiguous-outcome case this project already takes seriously (see [Relocating a Machine](README.md#relocating-a-machine)). New `Agent.migrationInFlightFor` lists `MachineMigrations` in the Machine's namespace and checks for one naming it via a small `isNonTerminalMigrationPhase` helper -- deliberately a fresh, package-local duplicate of the shape `internal/controller`'s `disruption.go` and `internal/uiapi`'s `overview.go` already each keep their own copy of, rather than a new shared cross-package dependency for one boolean, matching the precedent `disruption.go`'s own comment already documents. When one is found, `reconcileMachine` returns an error instead of proceeding into `cleanup` -- surfaced via the existing per-item `status.phase: Error` + `kairon_reconcile_item_errors_total` machinery, unchanged -- and the deletion is retried automatically on a later tick once the migration reaches `Succeeded`/`Failed`/`Blocked`. Fails closed on a migration-list lookup error too (an already-granted `kairon-node` RBAC read, not a new grant, unlike the earlier `MachineQuota` self-check's fail-open posture) since the risk here -- corrupting a live transfer -- is worse than a Machine deletion pausing for one extra tick on a transient error. 2 new tests (`TestCleanupRefusesDeleteWhileMigrationInFlight`, `TestCleanupProceedsWhenNoMigrationInFlight`).
- [x] `MachineSnapshot` deletion no longer abandons a frozen guest filesystem mid-quiesce: found while auditing kairon's other deletion paths for the same shape as the Machine-delete-during-migration race just fixed above. `MachineSnapshot` had no finalizer at all, so deleting one while `status.phase` was `Freezing`/`Thawing` -- meaning `kairon.zyvor.dev/quiesce-request` was still asking `kairon-node` to hold the target Machine's guest filesystem frozen via real `guest-fsfreeze-freeze` -- simply removed the object from Kubernetes with nothing left to ever clear that annotation. `kairon-node`'s own `reconcileGuestQuiesce` only thaws in response to the request annotation being cleared (by design, so a stuck-frozen guest is never silently abandoned -- see `docs/guides/machine-snapshot-quiesce.md`'s own "never silently abandon" language for the normal completion path), so an orphaned request left the guest's filesystem frozen indefinitely: every write blocked, with no MachineSnapshot object left in the cluster to even show why. New `internal/model.FinalizerSnapshotQuiesce`, added by `requestGuestFreeze` the moment a freeze is first requested (never before -- a snapshot that hasn't reached that point, or whose target Machine never enables `spec.guestAgent.enabled`, deletes exactly as before this existed). New `reconcileSnapshot` deletion branch (`reconcileSnapshotDeletion`): while the finalizer is present, requests a thaw (clearing `quiesce-request`, the same as normal completion) and holds the finalizer until `kairon-node` confirms it via `quiesce-status` -- retried indefinitely, matching `awaitGuestThaw`'s own posture exactly -- before finally removing the finalizer and letting deletion proceed. Also removes the finalizer immediately if the target Machine is gone too (nothing left to thaw). New `kube.Client.PatchMachineSnapshot` (metadata patch, mirroring `PatchMachineMigration`'s existing shape) needed to add/remove the finalizer. 6 new tests covering: finalizer added on first freeze request, deletion is a no-op without the finalizer (byte-for-byte prior behavior), deletion requests a thaw while still frozen on its behalf, deletion waits for thaw confirmation before removing the finalizer, deletion removes the finalizer once the guest is confirmed not frozen on its behalf, and deletion removes the finalizer immediately when the target Machine is gone too. See `docs/guides/machine-snapshot-quiesce.md`'s new "Deleting a MachineSnapshot while the guest is frozen" section.
- [x] A live migration always resumes the source's own network dataplane when the migration stops short of a destination commit: found while checking the same "delete/stop ignores an in-flight async operation elsewhere" shape that motivated the Machine-delete-during-migration and MachineSnapshot-delete-during-freeze fixes above, applied this time to a case that needs no deletion at all to trigger. `reconcileMigration` calls `Flux.NetworkMigrationQuiesce`/`NetworkMigrationExport` on the source runtime the moment a live migration starts, but nothing in the codebase ever called the matching `Flux.NetworkMigrationResume` on the *source* side -- only the destination resumes its own network, in `internal/migration/network.go`'s `NetworkAwareDestination.Commit`, which never runs unless the migration actually reaches a destination commit. Every other outcome -- the destination has no live migration backend (`Blocked`), an unsupported `spec.mode`, a source transfer that never started, or one that fails or is cancelled mid-flight (by far the most common real-world case) -- left the source's own network quiesced indefinitely, with the source Machine itself still the real, running VM throughout (unlike the destination, which is simply abandoned). New `Agent.resumeSourceNetworkQuiesce` (best-effort, log-only on failure, matching `NetworkMigrationQuiesce`/`Export`'s own established convention) is now called from all four of those exit points. Deliberately not called from `reconcileNeedsRecovery` -- an ambiguous commit must never be touched further by anything but an operator's own attested decision, the same principle that already governs that whole code path. 2 new tests: a blocked migration and a failed mid-flight transfer each prove the source's network is actually resumed, not just that the migration's own status phase is correct. See [Relocating a Machine](#relocating-a-machine) in README.md.
- [x] Bound a prepared destination live-migration session's lifetime independent of the source (opt-in, off by default): closes the one path that was still open after the network-quiesce fix directly above -- the *source* `kairon-node` crashing or being fenced between a successful `Prepare` and ever calling `Commit`/`Abort`, leaving the destination's reserved incoming-QEMU process with nothing to release it (no lease, no heartbeat, no reaper). Distinct from `NeedsRecovery` (covers an ambiguous *commit result*, not an abandoned *pre-commit* session). Fixed with a heartbeat/TTL added to the prepare/commit/abort protocol itself, exactly as previously scoped: a new `POST .../{id}/heartbeat` renews a "Prepared" session's `UpdatedAt`; `internal/agent`'s `reconcileMigration` calls it once per reconcile tick from the same per-tick branch that already polls `SourceMigrator.Status` -- best-effort, matching `resumeSourceNetworkQuiesce`'s own convention, never failing the reconcile over a missed heartbeat. `migration.Server.HeartbeatTTL` (wired via `kairon-node`'s new `--migration-heartbeat-ttl` flag, zero by default) bounds how long a "Prepared" session may go without a heartbeat before `RunReaper`'s background loop (checking once every `TTL/4`, floored at 10s) calls `ReapStaleSessions`, which self-aborts it via the existing `Driver.Abort` path and marks it `Aborted` with a reason naming the timeout. Zero `HeartbeatTTL` is byte-for-byte prior behavior -- no reaper goroutine starts at all, matching this project's own opt-in-by-default convention for every new capability-granting behavior. A real race was identified and closed before this could ship: `transition()` (commit/abort) does `Store.Get` -> a real `Driver.Commit`/`Driver.Abort` call (genuine wall-clock time) -> `Store.Put`: a concurrent reap seeing a session still `Prepared` with a stale `UpdatedAt` during that exact window could wrongly abort a destination that is, at that instant, legitimately coming up. Fixed with a per-session-ID mutex (`Server.lockFor`) held for the entire Get-Driver-call-Put critical section in both `transition()` and the reaper's own abort attempt, so the two can never interleave for the same session; `reapOne` also re-fetches and re-checks staleness *under* that lock, closing the List()-then-check TOCTOU window too. Proven with a dedicated test that forces the exact interleaving via a controllable-blocking fake driver (`TestReapStaleSessionsDoesNotRaceWithConcurrentCommit`), run repeatedly under `-race`. `Store` gained a new `List()` method (`FileStore.List()` skips any single corrupt session file rather than failing the whole reap pass) -- a breaking interface change with only the one real implementer in this codebase. No Helm wiring yet (flag-only) -- a documented follow-up, not forced into this same pass.
- [x] Fail closed on a `crypto/rand` failure minting a console ticket: `issueConsoleTicket` (`internal/uiapi/console.go`) discarded `rand.Read`'s own error via `_, _ = rand.Read(buf)`, unlike this codebase's own sibling implementation for an equally security-sensitive random value -- `randomNonce` in `oidc.go`, which already checks it. `crypto/rand.Read` only guarantees a full, unpredictable fill of its buffer when it returns a nil error; on the rare failure it can leave part or all of the buffer at Go's zero value, meaning a failed read could have minted a partially- or entirely-predictable ticket granting a live VNC/text console session, silently returned as if it were a normal, secure one. `issueConsoleTicket` now returns `(string, error)` and `handleConsoleTicket` responds `500` with no ticket issued on failure, instead of ever handing out one built from a short/zero-filled buffer. See SECURITY.md's "VNC console" section.
- [x] Scheduler failure messages name the blocking placement constraint, not just "no nodes match": `internal/scheduler.eligible` returned a bare boolean, so `Choose`'s own error when every node was filtered out (`"no Ready Kairon-capable nodes match placement constraints"`) carried zero information about which of `architecture`/`nodeSelector`/affinity/anti-affinity/`cpuPinning` capacity actually excluded them -- an operator debugging a stuck `Machine` had to guess. `eligible` now also returns a short reason string per exclusion; `Choose` aggregates per-distinct-reason counts across every filtered-out node into one deterministic, sorted message (e.g. `2 node(s): nodeSelector zone=us-east not satisfied; 1 node(s): cpuPinning requests 8 vCPU(s), only 4 free`). Purely diagnostic -- no scheduling decision changes, only what the error names when nothing is eligible. See `docs/guides/machine-placement.md`.
- [x] `kairon-ui` dashboard pages for `MachineQuota`/`MachineDisruptionBudget`/`MachineSet`/`MachineInstanceType`/`MigrationPolicy` (read-only): closes the "no dashboard button yet" half of the gap this file's own read-only-API-routes entry left open -- `GET /api/v1/quotas`/`disruption-budgets`/`machinesets`/`instancetypes`/`migration-policies` had no corresponding page in `web/`, so an operator who wanted to see quota usage or disruption-budget state without `kaironctl`/`kubectl` had nowhere to look. Five new pages (`web/src/pages/{Quotas,DisruptionBudgets,MachineSets,InstanceTypes,MigrationPolicies}.tsx`), each following the exact `api<T[]>(...)` + `setInterval` polling shape every existing list page already uses, with a new shared `web/src/components/ResourceTable.tsx` generic table renderer -- introduced specifically because five near-identical list-only tables landed in one pass, a narrow, deliberate exception to this frontend's usual per-page hand-copied `<table>` markup, not a general abstraction push. `Nav.tsx`'s `Page` union and `App.tsx`'s page-map both gained five new entries. Read-only, matching the backend routes exactly -- no create/edit/delete form, and `MachineInstanceType`'s own still-open "no picker in the Machine-create form" gap is unaffected (an operator can now at least browse instance types in the dashboard, just not select one when creating a Machine there). See `docs/guides/machine-quotas.md`/`machine-disruption-budgets.md`/`machine-sets.md`/`machine-instance-types.md`/`migration-policies.md`'s new "Dashboard" mentions.
- [x] `kaironctl create`/`scale`/`edit` for `MachineSet`/`MachineInstanceType`/`MigrationPolicy`: closes the "kubectl apply/YAML only" gap for the three kinds `kaironctl get`/`describe`/`delete` already covered but couldn't create or mutate -- every other kind (`Machine`, `MachineMigration`, `MachineSnapshot`, `MachineSnapshotRestore`) already had a `create` verb. New `internal/kube.Client` methods (`CreateMachineSet`/`PatchMachineSet`, `CreateMachineInstanceType`, `CreateMigrationPolicy`/`PatchMigrationPolicy`), each a direct copy of the existing `Create*`/`Patch*` shape for their neighboring kinds. `kaironctl create machineset|instancetype|migrationpolicy NAME [flags]` is dispatched from the existing `create` verb by matching `args[0]` against a fixed keyword list -- everything else falls straight through to the original, unchanged `create NAME --image PATH [flags]` (Machine) behavior, so `create` stays exactly one verb rather than needing a breaking new positional convention; the one real, documented tradeoff (mirroring `resourceKindAndName`'s own accepted one for `get`/`describe`/`delete`) is that a Machine literally named `machineset`/`instancetype`/`migrationpolicy` can't be created through this bare form -- `kubectl apply` remains the escape hatch, same as for every field these flags don't cover. A new shared `machineSpecFromFlags` helper (extracted from `cmdCreate`'s own pre-existing flag-to-struct mapping, with no behavior change to plain `create`) builds `create machineset`'s per-replica `Template.Spec` the identical way a plain `create machine` builds a Machine's, so the two can never drift apart on what a flag maps onto. New `--label`/`--selector` repeatable `key=value` flags (`parseKeyValues`, the map-flag counterpart to `parseForwards`'s existing `hostPort:guestPort` splitting) feed `MachineTemplate.Labels`/`MigrationPolicySpec.Selector`. `kaironctl scale machineset NAME --replicas N` and `kaironctl edit migrationpolicy NAME [--bandwidth-mbps N] [--max-concurrent N]` are new, narrowly-scoped verbs (the only two kinds with a sensible scale/edit operation for a first cut) with their own lightweight `KIND NAME` positional split rather than reusing `resourceKindAndName` (which is shaped for `get`/`describe`/`delete`'s "1 arg defaults to machine" convention, not a verb that always requires an explicit kind plus trailing flags); `edit` only ever patches the specific fields whose flag was actually passed on that invocation (via `flag.Visit`, never each flag's zero-value default), so an omitted flag can never clobber an already-configured value back to zero. `create instancetype`/`create machineset`/`create migrationpolicy` all cover the flag-friendly subset of their spec only -- anything nested or complex (placement, device claims, security, per-volume claims on a MachineSet's template) still needs `kubectl apply`/YAML, the same limit `create machine` already had. See `docs/guides/machine-sets.md`/`machine-instance-types.md`/`migration-policies.md`'s new `kaironctl create`/`scale`/`edit` examples.
- [x] `MachineSnapshotSchedule` (first cut): closes a real, previously entirely-missing capability -- neither this repo nor FluxVM had any backup-automation primitive at all, a `MachineSnapshot` was always a one-shot, operator- or script-triggered object. New namespaced CRD, structurally modeled on `MigrationPolicy` (a selector-scoped policy object, `internal/model/machinesnapshotschedule.go`/`charts/kairon/crds/machinesnapshotschedules.yaml`): `spec.selector` matches Machines the same `LabelsMatch` way `MigrationPolicy`/`MachineDisruptionBudget` already do, `spec.intervalSeconds` (minimum 60) is a plain wall-clock interval -- deliberately not real cron syntax, matching this project's Go-stdlib-only bias (no new cron-parsing dependency) and its established "honestly-labeled simpler first cut" pattern (`MigrationPolicy`'s own `bandwidthMbps`/`maxConcurrent` are plain scalars for the same reason). A pure `Spec.Due(lastRun, now time.Time) bool` method (directly unit-tested, including the "never run yet" case, which is immediately due rather than waiting a full interval after creation) keeps the actual "is it time" logic out of the reconciler entirely. `internal/controller/machinesnapshotschedule.go`'s `reconcileMachineSnapshotSchedules` follows `reconcileDisruptionBudgetsStatus`'s own exact "list all (tolerant of the CRD not being installed), compute, patch status" shape, wired into `Controller.Reconcile` alongside it -- for each due schedule, creates one `MachineSnapshot` per matching Machine (name `<schedule-name>-<unix-timestamp>`, `spec.volumeSnapshotClassName` passed straight through from the schedule), logging and counting (`kairon_reconcile_item_errors_total{kind="snapshotschedule"}`) a per-Machine create failure without ever skipping the rest of that schedule's matches or failing the whole tick; `status.lastRunTime`/`lastRunSnapshotCount`/`lastRunError` are patched once per due schedule even when it matched zero Machines, so a schedule with nothing to snapshot this tick doesn't re-fire every tick forever. `spec.suspend` pauses a schedule without deleting it. `kairon-controller`'s ClusterRole gained a new `get/list/watch/patch` rule on `machinesnapshotschedules`/`machinesnapshotschedules/status`, plus (a real, confirmed gap) a `create` verb on `machinesnapshots` it never had before -- only `kairon-ui`'s separate ClusterRole had that. `kaironctl` gained a 10th resource kind across `get`/`describe`/`delete`'s existing dispatch, plus `create snapshotschedule NAME --selector k=v --interval-seconds N [--volume-snapshot-class NAME]` and `edit snapshotschedule NAME [--suspend true|false] [--interval-seconds N]` (extending the existing `cmdEdit` verb, which now supports two kinds instead of one, each with its own flag set). No admission webhook, deliberately -- same reasoning `MigrationPolicy`'s own guide already gives: this CRD doesn't gate any other object's admission, it only creates new objects on a timer. Real, plainly documented first-cut limits: no real cron syntax, no jitter/stagger across schedules sharing an interval, and no automatic snapshot pruning/retention -- old snapshots accumulate forever unless something else deletes them, a real, separate, un-implemented follow-up. See `docs/guides/machine-snapshot-schedules.md`.
- [x] `MachineSnapshotSchedule` retention (`spec.keepLast`, first cut): closes the "no automatic snapshot pruning" limit the schedule's own guide named plainly when it shipped -- without this, every scheduled run's `MachineSnapshot`s accumulated forever. `spec.keepLast`, when set, bounds how many of a schedule's own ready-to-use snapshots survive *per Machine*: once a Machine has more than `keepLast`, `pruneScheduledSnapshots` (`internal/controller/machinesnapshotschedule.go`) deletes the oldest (by `metadata.creationTimestamp`) right after each due run's new snapshot is created. Correctness rests on two deliberate exclusions from what counts as a pruning candidate: every `MachineSnapshot` a schedule creates is now stamped with a new `kairon.zyvor.dev/snapshot-schedule: <name>` label (`model.SnapshotScheduleLabel`) at creation time, so pruning can reliably tell its own snapshots apart from a manually-created one or a *different* schedule's -- a snapshot with no such label, or the wrong schedule's label, is never touched regardless of age; and only `status.readyToUse` snapshots are ever counted toward or against the limit, so a snapshot still `Freezing`/`Thawing`/`Pending` is never itself a deletion candidate and never displaces an older one out of the count -- a `keepLast: 1` schedule therefore never has a moment with zero completed backups, since the old one is only ever deleted once a newer one has actually finished. Zero (the default, and every schedule's behavior before this field existed) never prunes anything. New `kaironctl create snapshotschedule ... --keep-last N` and `kaironctl edit snapshotschedule NAME --keep-last N` (extending `cmdEdit`'s existing flag-actually-set tracking). Deliberately count-only and per-Machine, not time-tiered ("one per day for a week, one per week for a month") -- a real, separate, un-implemented follow-up if ever needed, not a hidden gap. See `docs/guides/machine-snapshot-schedules.md`'s new "Retention" section.
- [x] `kairon-ui` gains a `MachineSet` delete action, first of the five dashboard pages to go beyond list-only: `DELETE /api/v1/machinesets/{namespace}/{name}` (`handleDeleteMachineSet`) mirrors `handleDeleteMachine`'s own any-authenticated-operator gate exactly -- no separate admin check, matching that a MachineSet delete is no more privileged than deleting a Machine directly, which any operator can already do. `MachineSet` was picked over `MachineQuota`/`MachineDisruptionBudget`/`MachineInstanceType`/`MigrationPolicy` because it's the one an operator manages as a day-to-day fleet-sizing operation -- `kaironctl create`/`scale`/`edit machineset` all landed this same session -- rather than a GitOps-managed policy object or a mostly-static reference value; the other four stay list-only for now, a deliberately narrow first slice rather than adding a write action to all five at once. Deleting a MachineSet has no cascade effect on Machines it already created: `MachineSet` carries no finalizer, so those Machines simply stop being managed and keep running as ordinary Machines -- the dashboard's confirm dialog says so explicitly rather than implying a full teardown. See README.md's "What ships today" section.
- [x] Dashboard visibility for `MachineSnapshotSchedule`: closes a gap this same CRD's own guide named honestly the moment it shipped -- `MachineSnapshotSchedule` (added earlier this batch) had `kaironctl get`/`create`/`edit`/`delete` support from day one, but zero visibility anywhere in `kairon-ui`'s REST API or dashboard, unlike the five older CRDs an earlier feature this session already wrapped. New `GET /api/v1/snapshot-schedules` (any-authenticated-operator, matching every other read-only cross-checkable resource) plus one write action: `PATCH /api/v1/snapshot-schedules/{namespace}/{name}/suspend`, toggling `spec.suspend` -- picked as the one dashboard mutation for the same reason `MachineSet` got a delete button and the other four CRDs didn't (see `internal/uiapi/fleet.go`'s own file-level comment): it's the action an operator reaches for as routine fleet management (pausing backups for a maintenance window), not a full spec edit -- `selector`/`intervalSeconds`/`keepLast`/`volumeSnapshotClassName` still need `kaironctl`/`kubectl`. New **Snapshot schedules** dashboard page lists every schedule's interval, retention, and last-run status alongside the toggle. See `docs/guides/machine-snapshot-schedules.md`.
- [x] `MachineSnapshotSchedule` gains `status.nextRunTime`: closes the gap between `status.lastRunTime` (when it *last* ran) and the question every operator actually has -- when will it run *next*. New `MachineSnapshotScheduleStatus.NextRunTime`, set every time a schedule actually fires (`internal/controller/machinesnapshotschedule.go`) to that run's `lastRunTime + intervalSeconds`, via a new pure `Spec.NextRunAfter(firedAt time.Time) time.Time` method (directly unit-tested, mirroring `Spec.Due`'s own pure-function shape) -- a schedule matching zero Machines this tick still gets it patched, exactly like `lastRunTime` already was, so a schedule with nothing to snapshot doesn't show a stale projection either. Deliberately a projection made once per actual fire, not a live countdown recomputed every reconcile tick: `MachineSnapshotSchedule`'s status is only ever touched when a schedule is `Due`, and adding a second tick that patches `nextRunTime` alone for every not-yet-due schedule would multiply this CRD's apiserver write volume for a value that's already a pure function of fields already in `status`/`spec`. The one real consequence of that choice: `nextRunTime` is never cleared or recomputed if a schedule is suspended sometime after it last fired, so the stored value can point at an already-passed timestamp while `spec.suspend: true`. Handled at *display* time, not by chasing it with more writes -- a new `formatNextRun` in both `kaironctl` (`internal/kaironctl/kaironctl.go`) and the dashboard (`web/src/pages/SnapshotSchedules.tsx`) checks the live `spec.suspend` flag first and shows `suspended`/`pending` instead of a stale timestamp, exposed as a new `NEXTRUN` column in `kaironctl get snapshotschedules` and a new **Next run** column in the dashboard's **Snapshot schedules** page. The CRD's own new `NextRun` printer column (`.status.nextRunTime`, for `kubectl get machinesnapshotschedules`) can't apply that same live-suspend check -- a real, honestly-named limit of the plain-printer-column mechanism, not something this project's own code can paper over; a `kubectl`-only workflow should check the `Suspend` column alongside it. See `docs/guides/machine-snapshot-schedules.md`'s new "When will it run next?" section.
- [x] `kaironctl describe snapshotschedule` gains a "what would fire right now" preview: closes a real, previously-unanswerable operational question -- before editing `spec.selector` or `spec.intervalSeconds` on an existing `MachineSnapshotSchedule`, there was no way to check what the *next* reconcile tick would actually do to it without either waiting for that tick or reading the Machine fleet's labels by hand and cross-referencing them against the schedule's selector yourself. `describe` for every other kind still uniformly raw-JSON-dumps the object, unchanged -- this is a narrow, deliberate exception scoped to the one CRD where the question is genuinely operational, not a generalized richer-describe change for every kind (which prior research confirmed this project has no existing pattern for). New `describeSnapshotSchedule` (`internal/kaironctl/kaironctl.go`) prints the schedule's usual JSON, then lists every Machine in the schedule's own namespace whose labels satisfy `spec.selector` (via the same `model.LabelsMatch` helper the controller itself uses), sorted for stable output, alongside whether the schedule is due right now -- evaluated by calling the exact same `Spec.Due(status.lastRunTime, time.Now())` pure function `reconcileMachineSnapshotSchedules` itself calls every tick, so the preview can never drift from what the controller will actually do. A selector matching zero Machines prints an explicit hint rather than a bare empty section, the same "don't leave an unexplained gap" instinct `keepLast`'s own retention guide follows. Real limit: it's a snapshot of *this instant* -- a Machine's labels, or the schedule's own selector/interval, can change between running `describe` and the next actual reconcile tick, so it's a preview, not a guarantee. See `docs/guides/machine-snapshot-schedules.md`'s new "Previewing what would fire right now" section.
- [x] `MachineSnapshotSchedule` gains `spec.startingDeadlineSeconds` (opt-in, off by default): closes a real gap this CRD had from day one -- however overdue a schedule was found (controller down for a maintenance window, this CRD's `status` reset by a reinstall, or just a very long outage), it always fired immediately, no matter how stale that catch-up run actually was. Kubernetes `CronJob` has faced this exact question and solved it with a field of the same name; `MachineSnapshotSchedule` now offers the identical opt-in escape hatch rather than inventing a new concept. A new pure `Spec.DeadlineExceeded(lastRun, now time.Time) bool` (`internal/model/machinesnapshotschedule.go`, directly unit-tested alongside `Due`/`NextRunAfter`, including the exact boundary instant) reports whether a due run's window -- `lastRun + intervalSeconds` -- is now more than `startingDeadlineSeconds` in the past; deliberately kept independent of `Due` itself (folding a second opt-in concept into `Due`'s own long-stable boolean contract would make every existing caller re-reason about a field most schedules never set) rather than merged into one combined method. `reconcileMachineSnapshotSchedules` checks it right after `Due` and, when exceeded, takes a third, distinct branch: no `MachineSnapshot` is created for that window at all, but `status.lastRunTime`/`nextRunTime` still advance to now (so the next tick starts counting a fresh interval instead of re-detecting the same missed window forever) and `status.lastRunError` records the skip -- surfaced through every place a run error already was (`kaironctl get`/`describe snapshotschedule`, the dashboard's own **Last error** column), no new plumbing needed there. Zero (the default, and every schedule's exact behavior before this field existed) never takes this branch -- an overdue run always fires, unchanged, so enabling it on an existing schedule is purely additive. `kaironctl describe snapshotschedule`'s own "what would fire right now" preview gained a third outcome to match -- `due, but will be SKIPPED` -- rather than folding a missed-deadline schedule into either its existing "due now" or "not due yet" cases, since it's neither. New `--starting-deadline-seconds` flag on `kaironctl create`/`edit snapshotschedule` (the `edit` flag follows the existing flag-actually-set convention, never clobbering it back to zero when omitted) and a new `StartingDeadline` CRD printer column. Real, plainly documented limit: the skip is all-or-nothing per due window, not per-Machine -- a schedule matching several Machines skips the entire window for all of them together, there's no partial "fire for some, skip the rest" grace period. See `docs/guides/machine-snapshot-schedules.md`'s new "Missed runs" section.
- [x] `kaironctl create`/`edit` gain `quota`/`budget`, closing the one real CLI-verb gap `MachineSet`/`MachineInstanceType`/`MigrationPolicy` didn't have anymore after this session's earlier `create`/`scale`/`edit` push: `MachineQuota` and `MachineDisruptionBudget` had `get`/`describe`/`delete` from day one, but no way to create or mutate either without dropping to `kubectl apply`/YAML -- the exact gap `MachineSet` et al. had before this same session closed it for them. `kaironctl create quota NAME [--max-machines N] [--max-total-cpu N] [--max-total-memory SIZE]` and `kaironctl create budget NAME --selector k=v (--min-available X | --max-unavailable X)` are new `cmdCreate` dispatch cases (`internal/kaironctl/kaironctl.go`), and `internal/kube/client.go` gains the `CreateMachineQuota`/`PatchMachineQuota`/`CreateMachineDisruptionBudget`/`PatchMachineDisruptionBudget` methods needed to back them -- direct copies of `MigrationPolicy`'s own five-method shape, swapping names/paths, since neither CRD's schema needed to change (both already allowed everything these flags set). Each `create` verb enforces the one invariant its own CRD schema can't express: quota refuses to create an object with every dimension unset (a `MachineQuota` capping nothing is never useful, matching the model's own doc-comment advice), and budget requires exactly one of `--min-available`/`--max-unavailable`, mirroring `MachineDisruptionBudgetSpec.DesiredHealthy`'s own documented "exactly one, not both or neither" contract instead of letting an operator create a budget that would fail that check on every future reconcile tick. `kaironctl edit quota NAME [flags]`/`edit budget NAME [--selector k=v] [--min-available X] [--max-unavailable X]` follow the same flag-actually-set convention every other `edit` verb already uses (`fs.Visit`, never a flag's zero-value default) so an omitted flag can never clobber an already-configured cap or bound back to zero; `edit budget` additionally refuses passing both bound flags in the same call, for the same reason `create budget` does, though switching an existing budget from one bound type to the other still needs a follow-up `kubectl`/YAML edit to clear the old field first -- a real, plainly documented first-cut limit of a plain merge-patch, not something this verb's simple flag-tracking can paper over on its own. See `docs/guides/machine-quotas.md`/`docs/guides/machine-disruption-budgets.md`'s new "Creating and editing via kaironctl" sections.
- [x] `Machine` gains `spec.priority`: a real, previously-missing scheduling primitive -- every Machine competing for a scarce reconcile tick (not enough Ready/capable nodes, or a `MachineQuota` at its cap) was admitted in whatever order the API happened to list them in, with no way for an operator to say "this one matters more." New `MachineSpec.Priority` (`int32`, `omitempty`, same "no fixed range, higher wins" convention as `PlacementSpec`'s own `preferredAffinity`/`preferredAntiAffinity` weight fields) and a new, directly unit-tested pure function, `scheduler.SortByPriorityDesc`, called once per `Reconcile` tick on the slice of still-unscheduled Machines right before `internal/controller`'s existing scheduling loop -- `sort.SliceStable`, so every Machine sharing a `Priority` (the overwhelming majority: the zero-value default, unchanged) keeps its original relative order, meaning a fleet that never sets this field schedules in exactly the order it always did. Deliberately an admission-*order* signal only, not preemption: it never evicts or re-schedules an already-running lower-priority Machine, however low, and it never influences *which node* an eligible Machine lands on (that's still `internal/scheduler`'s own load/affinity/topology-spread scoring, entirely unchanged) -- a real, honestly-scoped first cut, matching this project's usual posture of shipping the simpler, lower-risk mechanism rather than a fuller preemption model with real cleanup-correctness stakes. `kaironctl create`/`create machineset` both gain a `--priority N` flag (the latter via the shared `machineSpecFromFlags` helper, so every replica a `MachineSet` creates inherits it), and the CLI's first-ever `edit machine` verb lands alongside it -- `kaironctl edit machine NAME --priority N`, the one Machine-spec field this project considers safe to patch after creation via a narrow merge-patch, since it only ever affects a future tick's admission order and never itself triggers a re-realization. See `docs/guides/machine-placement.md`'s new "Scheduling priority" section.
- [x] New `scripts/check_rbac_coverage.py`, run in CI alongside `helm lint`/`helm template`, plus the one real gap it found and fixed on first run: closes the same class of bug the just-shipped `kairon-ui` ClusterRole fix was, found the same way -- by hand, reading `charts/kairon/templates/all.yaml` next to the actual `internal/kube.Client` calls each ServiceAccount's own code makes -- except this time automated, so a third one can't slip in silently the same way. The gap it found on its first run: `kairon-controller`'s ClusterRole grants `create` on `machinesnapshots` (for `MachineSnapshotSchedule` firing) but never `delete`, even though that same CRD's `spec.keepLast` retention (`pruneScheduledSnapshots`, `internal/controller/machinesnapshotschedule.go`) has called `Kube.DeleteMachineSnapshot` since the day `keepLast` shipped -- every prune attempt against a real apiserver has 403'd, silently (best-effort, logged-not-failed by design), since then. Exactly the reason it went unnoticed twice now: `internal/kube`'s fake `httptest.Server` test doubles never enforce RBAC, so `go test ./...` is silent about a real cluster's 403 either way. The script itself derives what each of `kairon-controller`/`kairon-node`/`kairon-ui` actually needs straight from `internal/kube/client.go`'s own source (which HTTP path/verb each exported method hits, not a guess from its name, so it can't drift as methods are added) and cross-checks that against a `helm template` render with every conditional RBAC flag turned on at once -- deliberately mirroring the exact gotcha (`ui.enabled`/`ui.token` gating kairon-ui's whole RBAC block) that let the first gap hide. Deliberately narrow, honestly-scoped: it never checks `kairon-csi-controller`/`kairon-csi-node` (neither calls `internal/kube.Client` at all -- they only speak the CSI gRPC interface) or `kaironctl` (runs under the operator's own kubeconfig, not a chart-granted ServiceAccount), never checks the `watch` verb (nothing in this codebase issues a real watch to derive that requirement from), and ignores `resourceNames` scoping (a Role scoped to one Secret/ConfigMap name counts as full coverage without checking the specific call site's name matches -- true in practice for every current call site). See `scripts/check_rbac_coverage.py`'s own module docstring for the full account.
- [x] `kaironctl top machines`/`top nodes`, a `kubectl top`-style live usage view: closes the gap between `status.resourceUsage` existing per-Machine (see `docs/guides/machine-resource-limits.md`) and there being any fleet-wide way to actually read it without listing every Machine and eyeballing the columns yourself. `top machines` prints each Machine's own CPU%/memory/disk-I/O row; `top nodes` rolls those same per-Machine samples up by `spec.nodeName` into a per-host hotspot view -- exactly the "which node is actually hot right now" question `kaironctl get machines` alone can't answer without an operator mentally grouping and summing rows. The grouping/summing itself lives in a new `model.AggregateUsageByNode` (`internal/model/usage.go`) rather than inline in `cmdTop`, specifically so `kairon-ui`'s own **Nodes** dashboard page could reuse the identical rollup logic (`GET /api/v1/nodes/usage`, `internal/uiapi`'s `handleNodeUsage`) instead of growing a second, independently-maintained copy that could drift from the CLI's answer -- the same shared-pure-function shape `model.LabelsMatch` already uses between the scheduler and every selector-filtering CLI verb. Deliberately no new metrics pipeline or controller-side aggregation: both surfaces just render data every Machine already reports. See `docs/guides/machine-resource-limits.md`'s new "Watching live usage" section and the dashboard table in README.md.
